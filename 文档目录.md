# token-burner · 项目文档目录

> 本文件是项目的文档总目录，逐项说明各文件/文件夹的作用。
> 生成时间：2026-08-28 ｜ 产品版本：规格 v0.3.1 全量实现（MVP 已交付）

---

## 一、核心文档（项目根目录）

| 文件 | 作用 |
|---|---|
| `Token消耗器_AI多智能体项目团队系统_开发规格文档_v0.3.1.docx` | **产品需求基线（原始版）**。定义全部功能需求、六层成本护栏、模块化治理、接口门禁等设计决策，是「产品是否做完」的唯一判定标准 |
| `Token消耗器_AI多智能体项目团队系统_开发规格文档_v0.3.1_合并版.md` | 同一规格文档的 Markdown 合并版，便于检索与开发过程中逐条比对 |
| `README.md` | 项目使用说明：安装、启动方式、安全与信任边界声明（进程级隔离 + 黑名单 + 超时，非容器沙箱） |
| `requirements.txt` | Python 依赖清单 |
| `.env.example` | 环境变量配置模板（LLM API Key 等，复制为 `.env` 后填写） |
| `conftest.py` | pytest 根配置：将 `code/` 加入 sys.path，保障交付物测试可运行 |

## 二、投资人汇报材料（项目根目录）

| 文件 | 作用 |
|---|---|
| `token-burner_投资人汇报.pptx` | 12 页投资人演示文稿：痛点 → 解决方案 → 工作管线 → 六层护栏 → 质量保障 → 工程完成度 → 产品形态 → 市场机会 → 竞争壁垒 → 路线图 |
| `token-burner_可行性研究报告.docx` | 七章可行性研究报告：执行摘要 / 市场分析 / 技术可行性 / 产品可行性 / 经济可行性 / 风险对策 / 结论建议 |
| `competitor-benchmark-analysis.pdf` | 竞品基准分析材料（市场调研参考） |

## 三、交付物

| 文件/文件夹 | 作用 |
|---|---|
| `release/token-burner.exe` | **桌面客户端（最终产物）**。独立可执行文件（75MB），双击即用；单进程零网络架构，兼容企业安全软件环境 |
| `client.html` | 客户端界面（单文件 HTML）。桌面模式经 pywebview 加载，浏览器模式由 API 服务同源托管；双模式共用同一套交互逻辑 |

## 四、源代码（`app/`）

### 4.1 顶层模块（`app/`）

| 文件 | 作用 |
|---|---|
| `main.py` | CLI 命令行入口：交互式需求输入、评估展示、管线执行 |
| `server.py` | API 服务（FastAPI）：评估路由 / 任务执行 / 中断恢复 / 反馈闭环 / 成本看板接口契约 |
| `desktop.py` | 桌面客户端入口：pywebview 窗口 + js_api 进程内桥（零 socket 架构） |
| `orchestrator.py` | 编排器：三分类路由、团队组建（三模型互异校验）、方案讨论双评审与收敛 |
| `pipeline.py` | 管道管理：任务启动、spec 确认、模块拆分、中断快照落盘与断点续跑 |
| `config.py` | 配置加载：模型列表、预算参数、护栏阈值、超时重试等集中管理 |

### 4.2 智能体层（`app/agents/`）

| 文件 | 作用 |
|---|---|
| `dev_loop.py` | 开发循环核心：模块开发 → 静态验证 → 接口门禁 → 执行 → 修复（≤5 轮）→ 反馈闭环；含不可信文本边界隔离 |
| `module_builder.py` | 模块构建：按 spec 生成模块目录、独立规格 md、接口地图 |

### 4.3 执行层（`app/execution/`）

| 文件 | 作用 |
|---|---|
| `executor.py` | Executor 抽象接口（双模执行的统一契约） |
| `safe_executor.py` | 安全审阅模式执行器：AST 静态检查 + LLM 逻辑审查，不真实执行 |
| `local_executor.py` | 自动验证模式执行器：危险预扫描（系统命令/网络/动态执行）+ 真实子进程执行 + 30s 超时熔断 |

### 4.4 基础设施层（`app/utils/`）

| 文件 | 作用 |
|---|---|
| `model_client.py` | LLM 统一客户端：litellm 封装、超时（120s）+ 指数退避重试、调用日志 |
| `budget.py` | 预算总闸（第 0 层护栏）：token 累计、90% 省 token 模式、超预算熔断落盘 |
| `similarity.py` | 循环检测（第 3 层护栏）：Jaccard 文本相似度 + embedding 语义相似度双道拦截 |
| `parse.py` | JSON 解析容错：围栏剥离、截断分块续写、LLM 辅助修复兜底 |
| `static_check.py` | 静态验证：语法检查、结构检查、测试文件存在性校验 |
| `interface_check.py` | AST 接口门禁：接口地图为唯一合法基线，违规引用即阻断 |
| `shared_check.py` | 共享库变更整包回归检查（改 `_shared` 触发全模块回归） |

### 4.5 工具与看板（`app/tools/`、`app/dashboard/`）

| 文件 | 作用 |
|---|---|
| `tools/file_manager.py` | 项目持久化：独立项目目录、中间文件、会话记录管理 |
| `tools/git_manager.py` | 交付物 Git 初始化与版本管理 |
| `tools/prompt_templates.py` | 提示词模板：各角色系统提示、安全规则注入 |
| `dashboard/cost_dashboard.py` | Token 成本看板：任务级消耗、按模型/阶段统计、预算占用比例、审计口径落盘 |

## 五、测试（`tests/`，共 34 个文件 / 472 项测试）

| 类别 | 文件 | 覆盖内容 |
|---|---|---|
| 管线核心 | `test_pipeline.py`、`test_orchestrator.py`（test_task_router/test_team_builder/test_discussion） | 三分类路由、团队组建互异校验、方案讨论收敛 |
| 开发循环 | `test_dev_loop.py`、`test_module_builder.py`、`test_feedback_loop.py` | 模块开发、修复循环上限、反馈闭环 |
| 护栏机制 | `test_budget.py`、`test_similarity.py`、`test_embedding_check.py`、`test_modular_gate.py` | 预算闸门、Jaccard/embedding 检测、模块化启用条件 |
| 验证执行 | `test_executor.py`、`test_local_executor.py`、`test_static_check.py`、`test_interface_check.py`、`test_shared_regression.py` | 双模执行、AST 门禁、共享库回归 |
| 基础设施 | `test_model_client.py`、`test_llm_resilience.py`、`test_parse.py`、`test_parse_fence.py`、`test_config.py` | 超时重试、JSON 围栏容错 |
| 交互与交付 | `test_api_server.py`、`test_desktop_bridge.py`、`test_file_manager.py`、`test_git_manager.py`、`test_deliverable_runnable.py` | API 契约、桌面桥接、交付物可运行性 |
| 加固项 | `test_interruption_recovery.py`、`test_route_reuse.py`、`test_call_log_lifecycle.py`、`test_prompt_injection.py`、`test_dashboard.py`、`test_minor_items.py` | 中断恢复、评估复用、日志生命周期、注入防护 |

> 运行方式：项目根目录执行 `python -m pytest tests/ -v`

## 六、脚本（`scripts/`）

| 文件 | 作用 |
|---|---|
| `make_investor_docs.py` | 生成投资人材料（PPT + 可行性研究报告），输出到项目根目录；依赖 `_docgen/` 本地库 |
| `e2e_demo.py` | 端到端演示脚本（需求 → 交付全流程示例） |
| `git_init_repo.py` / `git_push_github.py` / `git_push_api.py` / `git_sync_remote.py` / `sync_local.py` | 交付物仓库初始化与远程同步工具集 |
| `dump_trees.py` / `debug_tree_diff.py` / `debug_build2.py` | 调试辅助（目录树导出、构建问题排查） |

## 七、资源与构建

| 文件/文件夹 | 作用 |
|---|---|
| `assets/icon.ico`、`assets/icon.jpg` | 应用图标 |
| `token-burner.spec` | PyInstaller 打包配置（onefile / noconsole / 图标 / client.html 资源嵌入） |
| `.venv-build/` | 打包专用虚拟环境（含 PyInstaller，与运行环境隔离） |

## 八、运行时生成目录

| 文件夹 | 作用 |
|---|---|
| `projects/` | 每个任务一个独立目录：code/、测试、接口地图、sessions/（状态快照）、logs/（成本报告） |
| `logs/` | 桌面客户端运行日志（desktop.log，排查问题用） |

## 九、临时调试产物（可安全删除）

以下为历史排查 / 同步过程遗留，与产品运行无关：

`api_push.log`、`debug.log`、`data_store.json`、`dump_head.json`、`dump_ours.json`、`probe_result.txt`、`push_verify.txt`、`pv.txt`、`simple_test.txt`、`sync_out.txt`、`sync_progress.log`、`tree_debug.txt`、空的 `investor_docs/`

---

## 快速导航

- **想了解产品** → 规格文档 docx → `README.md`
- **想运行产品** → 双击 `release/token-burner.exe`（需先在 `.env` 配置 API Key）
- **想看代码** → `app/pipeline.py`（流程主线）→ `app/agents/dev_loop.py`（核心循环）
- **想验证质量** → `python -m pytest tests/ -v`
- **给投资人汇报** → `token-burner_投资人汇报.pptx` + `token-burner_可行性研究报告.docx`
- **重新打包** → `.venv-build` 环境下用 `token-burner.spec` 执行 PyInstaller
