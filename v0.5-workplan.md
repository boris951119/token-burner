# token-burner v0.5 工作计划（Beta · 业余时间 · AI 实现 / 用户验收）

> 依据：`v0.5.md`（Beta 任务清单，19 项）。承接 v0.4 全部 P2 顺延项。
> 节奏：沿用 v0.4 惯例——按批次排队而非按周排期，每批次是可独立提交、可独立验收的最小闭环。
> 分工：AI 负责实现与测试，用户负责验收（审代码 + 真实跑一遍）后提交。
> 基线：v0.4.0-alpha（500+ 测试用例，P0 全清）。

---

## 0. 全程约定（沿用 v0.4）

1. **批次闭环**：新增测试通过 + 全量回归绿（用例数不回退）+ 用户验收 + git 提交。
2. **开关保护**：Researcher（researcher_enabled）、联网调研（researcher_web_enabled）、模式推荐（mode_recommend_enabled）一律缺省关闭，关闭态行为与 v0.4 完全一致。
3. **不动管线编排**：Researcher 是「前置可选角色」——经触发器在管线前/修复环旁路注入，不改 Pipeline 主流程。
4. **client.html 单文件架构**继续；可视化用纯 SVG + v0.4 组件库 token，不引前端框架。
5. **文档随行**：CHANGELOG 每批次追加。
6. **推送注意**：git 原生 push 优先，静默失败则回退 `scripts/git_push_api.py`，推送后必须查询远端 ref 验证（v0.4 期教训）。

---

## 批次总览

| 批次 | 内容 | 优先级 | 预估会话数 | 硬依赖 |
|------|------|--------|------------|--------|
| V0 | M10-1/2/3/4 Researcher 降级版闭环（骨架/触发器/注入/预算缓存） | P0 | 3 | 无 |
| V1 | M11-1/11-2 可视化（监控面板升级 + 对话流图） | P0/P1 | 2~3 | 无 |
| V2 | M11-3 模式智能推荐 + M12-1 任务取消 + M12-5 A/B 框架产品化 | P1 | 2 | V0（推荐依赖历史数据） |
| V3 | M12-2 镜像预热 + M12-3 Docker 引导 + M12-4 插件配置页 | P1/P2 | 1~2 | 无 |
| V4 | M12-6/7/8/9/10 体验与生态小项（逐个独立提交） | P2 | 2 | 各自 |
| V5 | M10-5 联网调研（视外部工具能力）+ M13-1 流水线 + M13-2 v0.5 发布 | P2/P0 | 2 | 几乎全部 |

合计约 12~14 次会话。V0/V1 完成即具备 v0.5 Beta 的核心差异化；其余批次可按时间弹性伸缩。

---

## V0 · Researcher 降级版闭环（P0）

**改动点**：
- `app/agents/researcher.py`：输入用户资料片段（或未来抓取结果）→ 结构化摘要（来源/版本/用法示例/坑点四段式，LLM 生成 + 程序校验字段完整）；
- 触发器（三条件，规格 4.5）：CLI/API 入参 `research: "auto"|"on"|"off"`；主 LLM 评估 reason 含「陌生技术栈」标注；修复失败 ≥2 轮时输出建议（不自动激活）；
- 注入链路：摘要经 `_sanitize_untrusted` 同构包裹（M7-6 教训：研究资料同样不可信）后注入 Dev/Test 的 System Prompt；注入点与 requirement 插值点复用同一治理模式；
- 预算与缓存：`research_budget`（默认 20k）独立 BudgetGuard 实例、计入 call_log；缓存键 `sha(技术栈+API+版本)`，SQLite 复用 embedding 缓存基建；
- 新配置：`researcher_enabled`（缺省 False）、`research_budget: int = 20_000`。

**验收**：
- [ ] `researcher_enabled=False` 全量回归绿（行为 = v0.4）
- [ ] 三触发路径各有测试；摘要四段式字段校验；超预算熔断；同查询二次零调用
- [ ] 真实任务：陌生技术栈需求 + 粘贴文档 → 交付物引用研究内容（手动验收）

---

## V1 · 高级可视化（P0/P1）

**改动点**：
- M11-1 监控面板升级：client.html 工作台新增「监控」子面板——阶段耗时条形（logs/ 审计数据）、token 消耗曲线（SSE tokens 事件累积）、模块状态全景（复用 v0.4 模块卡）；
- M11-2 对话流图：纯 SVG 节点图——PM / 评审A / 评审B / 收敛 / Dev / Test 节点 + 消息流向边；数据源 = 方案讨论阶段的消息记录（v0.4 call_log 已存全部消息）；
- 插件侧同步消费（复用 SSE 通道，不新增端点；如需消息明细补只读端点 `GET /api/project/{id}/messages`）。

**验收**：
- [ ] 真实任务渲染正确：节点/边与日志一致；面板数据与 cost_report 口径一致
- [ ] 两视图（client.html / 插件）渲染一致；无 JS 报错

---

## V2 · 智能推荐 + 运维核心（P1）

**改动点**：
- M11-3 模式推荐：`GET /api/recommend?requirement=…`——按历史项目（sessions/ + cost_report）统计相似需求成功率与成本，输出 `{mode, budget, reason}`；推荐理由引用数据；前端发起时展示推荐（可覆盖）；
- M12-1 任务取消：`DELETE /api/tasks/{id}`——pending 直接标记 CANCELLED；running 用协作式取消（任务体周期检查取消旗标，复用 BudgetGuard 检查点）；重启后 running 且无心跳 → 标记僵尸并清理；
- M12-5 A/B 框架产品化：诉求集外置 JSON + `--out` 报告归档（承接 v0.4 `scripts/ab_triage_eval.py`）。

**验收**：
- [ ] 取消：排队任务立即取消；执行任务在检查点退出且现场可恢复
- [ ] 推荐理由引用具体历史数据；无历史时缺省 safe
- [ ] 全量回归绿

---

## V3 · Docker 运维与插件配置（P1/P2）

**改动点**：
- M12-2 镜像预热：`POST /api/prewarm`（python/node 镜像 pull + 常驻容器预建或镜像缓存校验）；启动 <5s 计时脚本；
- M12-3 Docker 检测引导：桌面端启动时检测 Docker → 未装则横幅指引 + 保持进程模式；
- M12-4 插件配置页：VS Code settings UI 补预算/默认模型配置项。

---

## V4 · 体验与生态小项（P2，逐个独立提交）

- M12-6 自定义路由策略（档位/阈值进 config）
- M12-7 设置页重设计（分组卡片，复用 M5-1 组件）
- M12-8 深色/浅色主题（token 双套 + prefers-color-scheme）
- M12-9 模型路由明细（call_log 档位 + 旗舰假设成本列）
- M12-10 插件消息协议契约测试（可顺延）

---

## V5 · 联网调研与发布（P2/P0）

**改动点**：
- M10-5 联网调研：搜索/文档抓取 API（可配置供应商），`researcher_web_enabled` 缺省关；失败回退资料注入；
- M13-1 GitHub Actions：pytest → PyInstaller → Release；
- M13-2 发布：CHANGELOG、PyInstaller 复验（≤80MB / <2s）、真实 A/B 报告归档。

**Done 定义**：见 `v0.5.md` 第七节。

---

## 风险清单

| 风险 | 应对 |
|------|------|
| Researcher 摘要质量不可控 | 四段式结构化 + 来源版本标注 + 注入前用户确认 |
| 联网调研供应商不稳定 | 降级链路：联网 → 资料注入 → 跳过（方向单一） |
| 流图渲染性能（消息多时） | 仅渲染阶段级节点 + 消息懒加载 |
| P2 批量合入回归 | 独立开关 + 每项独立提交 |
| 网络对 github/pypi 间歇中断 | git push 失败回退 API 推送；pip 用国内镜像 |

---

## Done 定义（v0.5）

- [ ] 全部 P0（4 项）完成；P1 完成率 ≥ 85%（M12-10 可顺延）
- [ ] Researcher 降级版闭环可用且预算/缓存生效
- [ ] 可视化面板 + 对话流图真实任务渲染正确（手动）
- [ ] 模式推荐可解释、可覆盖
- [ ] 全量测试基线（500+）无回退，新增 ≥60 用例
- [ ] CHANGELOG 与升级说明就绪；PyInstaller 复验通过
- [ ] 新能力全部缺省关闭，关闭态行为与 v0.4 一致（回归保证）
