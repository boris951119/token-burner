# token-burner v1.0 工作计划（Release · 质量收敛 / AI 实现 / 用户验收）

> 依据：`v1.0.md`（Release 任务清单，12 项）。版本主题：从 Beta 到 Release 的质量收敛。
> 节奏：沿用惯例——按批次排队而非按周排期，每批次是可独立提交、可独立验收的最小闭环。
> 分工：AI 负责实现与测试，用户负责验收（审代码 + 真实跑一遍）后提交。
> 基线：v0.5.0-beta 验收闭环（833 项测试 / 826 passed / 0 failed）。

***

## 0. 全程约定（沿用 v0.4/v0.5）

1. **批次闭环**：新增测试通过 + 全量回归绿（用例数不回退）+ 用户验收 + git 提交。
2. **行为兼容**：所有新能力缺省行为等价或严格收紧（M14 是「收紧」：以前放行的断裂
   现在被拦截——这是修复不是回归）；`contract_style` 缺省 `function` 与现状一致；
   `target_platform` 缺省 `windows`（本机交付环境，v0.5 实测教训）。
3. **不动管线编排**：M14/M15 全部落在「守卫、门禁、提示词」层，不改 Pipeline 主流程
   与状态机。
4. **证据文化**：每个修复批次必须用 v0.5 的真实失败场景做回归取证（断裂复现/
   file\_utils 重放），不带「应该好了」的口头结论。
5. **文档随行**：CHANGELOG 每批次追加；基准数据一律落盘 `logs/bench_v1/`。
6. **推送**：token 已入 `.env`，`SKIP_LOCAL_SYNC=1 python scripts/git_push_api.py
   https://github.com/boris951119/token-burner`；推送后查远端树 blob sha 验证。

***

## 批次总览

| 批次         | 内容                                                                                              | 优先级   | 预估会话数 | 硬依赖  |
| ---------- | ----------------------------------------------------------------------------------------------- | ----- | ----- | ---- |
| V0 机制堵漏    | M14-1/2（\_shared 符号合并守卫 + 全局链接门禁）                                                               | P0    | 2     | 无    |
| V1 收敛手段    | M14-3/4（平台约束+黑名单）+ M15-1/2（风格约束+报告指导）                                                           | P0/P1 | 2\~3  | 无    |
| V2 自适应与扫描  | M14-5/6/7（评审追加：平台上移门禁链+resume 预算恢复+LLM 逻辑审查）+ M15-3/4（风格可配置+上下文增强）+ M16-1（JS 扫描）+ **基准试跑 3 任务** | P0/P1 | 3\~4  | V1   |
| V3 Release | M17-1 全量基准 10 任务（通过率 ≥60% 门槛）+ M17-2/3 发布物                                                      | P0/P1 | 2\~3  | 几乎全部 |

合计约 8\~10 次会话。V0/V1 完成 = 机制缺口全堵 + 收敛手段就位；V2 中期抽查
设 50% 准入线，不达标回炉 M15，避免带病进 V3 烧全量基准预算。

***

## V0 · 机制堵漏（P0）：\_shared 合并守卫 + 全局链接门禁

**改动点**：

- `app/tools/file_manager.py`：`write_shared_file` 落盘前 AST 符号级合并——
  新版缺失的既有顶层符号自动保留（防丢函数根因修复）；同名内容变更采用新版
  并记 diff 日志；LLM 显式 `# DELETED: <name>` 注释标记的符号才允许删除；
  SyntaxError 回退整文件覆盖（旧行为，不阻塞流程）；

- `app/utils/static_check.py`（或新 `link_check.py`）：全局链接门禁——对全部已落盘
  模块（**含 FROZEN**）解析跨模块与 `_shared` import，被引符号必须存在于来源文件；
  缺失 → 阻断当前模块，输出「文件+符号+来源」精确清单；增量解析缓存（仅变更文件重解析）；

- `dev_loop` 静态验证链接入链接门禁（顺序：语法 → 静态 → 链接 → 接口门禁 → 执行）。

**验收（证据驱动）**：

- [x] 复现 v0.5 断裂场景：手工重放「book\_validator 依赖 validate\_isbn，后续模块
  输出丢该函数的 \_shared/utils.py」→ 合并守卫保留函数 + 链接门禁即使守卫失效也能拦截
  （取证脚本 `scripts/_v0_evidence.py`，见 CHANGELOG V0 批次）

- [x] 合并守卫三态单测：丢符号保留 / 同名变更采用新版 / DELETED 显式删除
  （`test_shared_merge.py` 14 项 + `test_link_check.py` 10 项）

- [x] 全量回归 833+ 绿（新增 ≥12 用例）（850 passed / +24，2026-09-02）

***

## V1 · 收敛手段（P0/P1）：平台约束 + 契约风格 + 报告指导

**改动点**：

- M14-3/4：`Settings.target_platform`（缺省 windows）+ write\_code/fix\_code/write\_tests
  提示词注入平台约束段；`scan_dangerous` 增加平台不可用模块清单（windows:
  fcntl/termios/pwd/grp/resource/tty 等；linux: msvcrt/winreg/pythonwin 等；
  any: 放行）；

- M15-1：split / initial\_proposal / converge\_spec 提示词增加 API 风格约定段
  （顶层可调用导出 + 正反示例），spec.md 生成携带该约定；

- M15-2：`interface_check` 失败输出升级——每个 missing 符号从契约入参/出参推导
  函数签名模板；extra 符号给处置指引；fix\_code 提示词消费该指导。

**验收**：

- [ ] file\_utils 场景重放（v0.5 实测 5 轮不收敛的类式实现契约）→ ≤2 轮收敛
  （随 V2 基准试跑统一取证，2026-09-04 备注）

- [ ] fcntl 场景重跑：生成代码改用平台可用锁方案（如 msvcrt / portalocker 思路）
  （随 V2 基准试跑统一取证，2026-09-04 备注）

- [x] 平台黑名单单测：windows 目标 `import fcntl` → BLOCKED；any 放行
  （`test_platform_policy.py`）

- [x] 全量回归绿（新增 ≥15 用例）（874 passed / +24，2026-09-02）

***

## V2 · 自适应与扫描（P1）+ 基准试跑

**改动点**：

- M15-3：`contract_style: function | class | auto`（缺省 function）；auto = 首轮实现后
  按实际代码顶层符号风格反推并回写契约（一次性 + 审计落盘 sessions/）；

- M15-4：修复轮提示词携带接口地图全文 + 已通过模块对本模块 API 的调用示例；

- M16-1：`scan_dangerous_js`（require/import 黑名单 + eval 族 + node --check），
  接入 LocalExecutor / DockerExecutor Node 链路；

- **基准试跑**：标准需求集（M17-1 定义的 10 个中先跑 3 个：CLI 工具/数据处理/
  陌生技术栈各 1），auto 模式，数据落盘 `logs/bench_v1/pilot/`。

**验收（中期准入线）**：

- [x] 3 任务试跑模块通过率 ≥50% —— **未达标（2026-09-05 终局结论：任务级
  3/3 succeeded，模块级 2/18=11%，报告 `logs/bench_v1/pilot_r5/
  bench_report.json`）**。四轮跑批（round-2f/r3/r4/r5）驱动 V2.1~V2.8 八批
  取证修复：机械性缺陷 18 模块跨三轮零复发；策略误杀（os.remove 直接
  冻结）被 V2.8 分级处置消除（T2 旧策略 2 例 → T3 新策略 0 例）；任务级
  韧性 100%（账户级限流/网关超时/褐色窗口全部存活）。剩余冻结经原因
  分类 100% 归因基座模型能力层（接口门禁不收敛/断言不收敛）——按 M17-1
  预案回炉点为「语义型→提示词迭代」与基座档位，机制层（M14/M15/M16）
  已收敛无待办。V3 全量基准前置条件：付费档旗舰 dev 模型就位（免费共享
  网关下的 11% 是对照基线而非产品上限，用户已计划用最先进模型亲测）。
  （2026-09-04 中期记录：试跑击穿并修复 6 个基础设施缺陷，round-2f T1
  死于免费档限流，详见 `logs/bench_v1/pilot/REPORT.md`）

- [x] contract\_style 三态单测；auto 风格回写审计记录落盘
  （`test_contract_style.py` 22 项）

- [x] JS 恶意样本集全拦截 + 双执行器集成测试绿（`test_js_scan.py` 28 项）

- [x] 全量回归绿（新增 ≥15 用例）（986 passed / +112，2026-09-04）

***

## V3 · Release（P0/P1）：全量基准 + 发布物

**改动点**：

- M17-1：10 任务全量基准（auto；Docker 可用则容器沙箱），报告归档
  `logs/bench_v1/full/`——逐任务通过率/修复轮/token/预算命中/冻结原因分类；
  **通过率 ≥60% 门槛**，不达标回炉（最多 2 轮，回炉点由冻结原因分类决定：
  风格型→M15、平台型→M14、语义型→提示词迭代）；

- M17-2：版本 1.0.0、CHANGELOG、README 口径更新（KPI 实测数）、exe 复验 ≤80MB、
  `v1.0.0` 标签触发 Actions；

- M17-3：文档目录 / 投资人材料数据刷新（成功率「待验证」→ 实测值，可选）。

**验收**：Done 定义（v1.0.md 第七节）全过。

***

## 风险清单

| 风险                                  | 应对                                              |
| ----------------------------------- | ----------------------------------------------- |
| M15 提示词改进效果不确定                      | V2 设 50% 准入线 + 冻结原因分类定向回炉，避免全量基准反复烧钱            |
| 合并守卫产生「僵尸函数」                        | DELETED 显式标记约定 + diff 日志审计；基准任务人工抽检 \_shared 内容 |
| 链接门禁性能                              | AST 毫秒级 + 增量缓存；实测超 500ms 再优化                    |
| 基准 API 成本（10×auto ≈ 500 万 token 上界） | 分批跑、单任务 500k 预算闸门兜底、跑批配置落盘可中止可复现                |
| 模型余额分型号耗尽（v0.5 教训）                  | 跑批前 ModelClient 探活；配置与余额变化落盘，保证报告可归因            |
| auto 风格回写与「决策归 LLM」哲学冲突             | 风格=工程约束；回写一次性+审计；function/class 可锁定关闭自适应        |

***

## Done 定义（v1.0）

- [ ] v0.5 三大质量缺口全部有「场景复现 → 修复 → 回归取证」闭环证据

- [ ] 标准需求集 10 任务模块通过率 ≥60%，报告归档 logs/bench\_v1/full/

- [ ] 预算命中率：基准任务超支率 <10%

- [ ] 全量测试基线 833 项无回退，新增 ≥47 项（≥880）

- [ ] 版本号 1.0.0；CHANGELOG/README/文档目录口径一致

- [ ] `v1.0.0` 标签触发 GitHub Actions 全绿（pytest → PyInstaller → ≤80MB → Release）

