# token-burner v1.1 工作计划（连接层 · 快速上手 / AI 实现 / 用户验收）

> 依据：2026-09-05 产品评审结论（UX 断层取证：前端「API 接口配置」面板为
> localStorage 死胡同，不参与任务执行）+ 用户需求「快速上手、快速配置 API、
> 接入市面上任何一种 LLM」。
> 主题：**连接层（Connection Layer）**——让用户用「我有一把某某家的 Key」思考，
> 不需要知道任何配置文件与命名规则。
> 节奏：沿用惯例——按批次排队，每批次是可独立提交、可独立验收的最小闭环。
> 分工：AI 负责实现与测试，用户负责验收（审代码 + 真实跑一遍）后提交。
> 基线：v1.0-V2.7（1017 passed / 7 skipped / 0 failed；bench_v1 五轮取证闭环）。

***

## 0. 全程约定（沿用 v1.0，新增两条）

1. **批次闭环**：新增测试通过 + 全量回归绿（用例数不回退）+ 用户验收 + git 提交。
2. **行为兼容**：.env 现有用户**零迁移**——注册表为空时全部行为与现状逐字节一致；
   探针/发现失败一律降级（手动填模型名兜底），不阻塞保存。
3. **不动管线编排**：连接层只扩展「模型解析」与「服务端点」，不改 Pipeline/
   DevLoop/门禁链主流程。
4. **安全不变式（硬性）**：API Key 只写不读——`GET` 永远返回掩码；密钥不落
   config.json、项目产物、日志、cost_report；密钥库文件加入 .gitignore。
5. **证据文化**：探针即取证（保存前真实调用验证 key/base/model 三要素）；
   每批附「90 秒上手」手动验收脚本。

***

## 批次总览

| 批次 | 内容 | 优先级 | 预估会话数 | 硬依赖 |
| --- | --- | --- | --- | --- |
| C1 连接注册表 | 密钥库 + CRUD 端点 + 掩码 + 运行时解析/热加载 + 会话 token | P0 | 2 | 无 |
| C2 探针与发现 | 添加即探针（3 秒知通不通）+ `/models` 自动发现 | P0 | 1~2 | C1 |
| C3 前端接线 + 首启向导 | 面板接真身 + 模板卡 + 三步向导 + 角色分配推荐组合 | P0 | 2 | C1/C2 |
| C4 本地模型路径 | Ollama / LM Studio 自动探测，零 Key 试用 | P1 | 1 | C2 |

***

## C1 · 连接注册表（P0）：后端真身

**改动点**：

- 新模块 `app/utils/connections.py`：
  - 连接模型 `{id, name, provider, base_url, api_key, models[], created_at}`；
  - 存储根目录 `secrets.local.json`（写入权限收紧为仅当前用户；**加入
    .gitignore**）；损坏回落空表（沿用 file_manager 容错风格）；
  - `list_connections()` 永远返回掩码（`sk-ab****ef`，复用前端 maskKey 语义
    移植到后端，单一事实源在后端）；
- `app/server.py` 新端点（沿用现有 18 端点风格 + Pydantic 请求模型）：
  - `GET /api/connections`（掩码列表）
  - `POST /api/connections`（新增；key 只进不出）
  - `DELETE /api/connections/{cid}`
  - 可用模型注册表 = `config.json` 预设 ∪ 各连接 `models[]`，任务创建
    （`POST /api/tasks` 的 `models` 参数）与 `GET /api/config` 同步消费；
- **运行时热加载**：`ModelClient` 解析 (base_url, api_key) 时按连接注册表
  实时查证（mtime 缓存），查不到回落 `.env`——新增模型/改 Key **免重启**；
- **会话 token**：首页加载时签发一次性 token（内存态），写端点校验
  `X-Session-Token` + Host 头白名单（127.0.0.1/localhost），防 localhost
  CSRF / DNS rebinding；GET 只读端点不强制；
- `Settings` 新增 `connections_path`（缺省 `secrets.local.json`）。

**验收（证据驱动）**：

- [ ] `test_connections.py`：CRUD + 掩码（GET 全程无明文 key）+ 损坏文件回落
  + key 不入 config.json/日志（字符串扫描断言）
- [ ] `test_api_server.py` 扩展：三端点集成；未带 token 的写请求 403；
  热加载——新增连接后**不重启**即可被任务创建消费（file_manager 式集成）
- [ ] 全量回归绿（新增 ≥12 用例）

***

## C2 · 探针与模型发现（P0）：添加即知通不通

**改动点**：

- `POST /api/connections/{cid}/probe`：用该连接发一次微型真实调用
  （max_tokens 极小；litellm 工厂注入可测），返回
  `{ok, latency_ms, verdict: ok|auth_failed|model_not_found|network_error,
  detail}`——**3 秒内告诉用户 Key 有效/余额不足/模型名错**；
  thinking 模型（glm-4.7 实测）content 与 reasoning 分离，探针只断言 content
  可解析；
- `GET /api/connections/{cid}/models`：对暴露 `/models` 端点的供应商
  （OpenAI 兼容/OpenRouter/Ollama）自动拉取模型清单；不支持的下发空表，
  前端回落手动输入；
- 前端：添加弹窗保存后自动触发探针，红绿灯反馈；模型字段优先下拉（发现
  结果），手输兜底。

**验收**：

- [ ] `test_connection_probe.py`：四类 verdict 的桩工厂用例（沿用
  test_api_server 的 llm_factory 注入模式）；探针调用不计入任务成本报告
- [ ] 手动验收：填入真实智谱 Key → 探针绿灯 ≤5s；故意填错 Key → 红灯
  `auth_failed`

***

## C3 · 前端接线 + 首启向导（P0）：90 秒从零到第一个任务

**改动点**：

- **面板接真身**：`client.html` 「API 接口配置」面板从 localStorage 改为
  C1 端点（删 `tb_api_configs` 本地逻辑与 DEFAULT_CONFIGS 假数据）；
- **模板卡**：内置供应商预设（智谱 / DeepSeek / Kimi / 通义兼容模式 /
  OpenRouter / Ollama），点卡片预填 name/base_url/推荐模型，用户只需贴 Key；
- **首启向导**：首启检测（注册表空 **且** .env 无可用 key）→ 全屏三步：
  ① 选卡片 ② 贴 Key + 探针 ③ 三角色分配 → 点火；.env 老用户自动跳过
  （**零迁移**）；
- **角色分配向导**：三模型互异（规格 3.3）以推荐组合呈现——
  🏆 旗舰组 / 💰 性价比组 / 🆓 本地组，同时展示「跨厂商互验」设计意图；
  分配结果随 `POST /api/tasks` 的 `models` 参数下发（bench_v1_run
  `--models` 已验证的通道）；
- **冻结原因前置展示**（2026-09-05 用户评审追加）：任务"成功"但模块 FROZEN
  时，原因目前埋在 `changelog/<module>/validation.md` 需翻文件——工作台模块
  卡片直接显示「哪个模块 / 为什么冻住（门禁类型 + 失败摘要）/ 建议下一步
  （查看 fix_history / 调高 max_fix_rounds / 换更强开发模型）」。数据链路
  现成（SSE `module_done` 事件已携带 status/message），改动集中在 client.html
  渲染层与摘要截取，不动管线。

**验收**：

- [ ] 手动验收脚本（写进 README「90 秒上手」节）：全新环境 → 打开首页 →
  向导自动弹出 → 智谱模板 → 贴 Key → 探针绿 → 推荐组合 → 点火 → 任务
  出现在工作台，全程 ≤90 秒、零配置文件
- [ ] `.env` 用户回归：现有启动路径不弹向导、行为与升级前一致
- [ ] 前端契约测试（client.html 关键路径 smoke：向导三步元素存在、面板
  走 fetch 而非 localStorage）
- [ ] 冻结展示：人为构造含 FROZEN 模块的任务 → 工作台卡片可见模块名/
  冻结原因摘要/建议动作三要素，无需翻 validation.md

***

## C4 · 本地模型路径（P1）：零 Key 试用

**改动点**：

- 探测本机 Ollama（:11434）/ LM Studio（:1234）——`GET /api/local-probe`，
  页面加载静默执行，命中即在向导/面板展示「🆓 本地模型（免费无限）」；
- Ollama 走 litellm `ollama/` 前缀直连；本地模型默认标记「质量未知」，
  推荐组合中仅入 🆓 组；
- 文档：README 增加「无 Key 试用」节（下载 Ollama → pull 模型 → 刷新即见）。

**验收**：

- [ ] `test_local_probe.py`：端口命中/未命中/超时三态（桩 socket）
- [ ] 手动验收：无 Key 环境装 Ollama → 向导出现本地组 → 本地模型点火跑通
  safe 模式小任务

***

## 风险清单

| 风险 | 应对 |
| --- | --- |
| 各供应商协议差异（json_mode/流式/thinking 字段） | 探针先行暴露差异；沿用 15.1 response_format 降级链；thinking 模型 content 分离已在 glm-4.7 实测验证 |
| 密钥文件权限 Windows 语义弱化 | 首版 0600 + .gitignore 双保险；v1.2 可选 DPAPI/keyring 增强 |
| 互异约束与「一个供应商一把 Key」冲突（用户只有一家 Key） | 向导允许同 Key 不同模型行互异（约束是模型级非供应商级，规格 3.3 语义不变） |
| 向导误伤 .env 老用户 | 首启检测三态（注册表/env/都空），仅「都空」进向导；回归用例锁定 |
| 探针被滥用为免费代调用 | 探针仅限保存时触发、max_tokens 钳制、频率限制（同连接 60s 一次） |

***

## Done 定义（v1.1）

- [ ] 前端「添加 API」端到端生效：添加 → 探针 → 模型下拉 → 点火，全程零配置文件
- [ ] 新用户「90 秒上手」手动验收通过并写入 README
- [ ] .env 老用户零迁移（回归绿）
- [ ] 安全不变式全绿：GET 掩码 / key 不入产物 / CSRF 防护 / 密钥库不入 git
- [ ] 全量回归基线 1017 项无回退，新增 ≥25 项
- [ ] CHANGELOG / 文档目录 / README 口径一致
