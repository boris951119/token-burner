# token-burner v0.5 Beta · 任务清单与优先级

> 版本定位：智能化与体验升级——Researcher 知识注入、过程可视化、模式智能推荐；同时承接 v0.4 全部 P2 顺延项，补齐企业级运维闭环
> 规划周期：约 10 周（业余时间节奏按批次排队，见 v0.5-workplan.md）
> 目标综合分：80 → 85
> 前置基线：v0.4.0-alpha 全量实现（500+ 测试用例，P0 全清、P1 除 M3-3 外全部完成）

---

## 一、版本目标

### 1.1 核心目标
- **Researcher Agent（规格第 4 章）**：可选前置角色——查阅文档、分析用法、输出结构化技术摘要（来源/版本/示例/坑点），注入 Dev/Test Agent 解决 LLM 知识过时；独立预算（默认 20k）+ 三元组缓存（技术栈+API+版本）；v0.5 先落地「用户提供资料注入」降级版，联网调研视外部工具调用能力灰度
- **高级可视化（规格 16.2/16.3）**：实时监控面板升级（阶段耗时、token 曲线、模块状态全景）+ Agent 对话流图（多智能体讨论过程可视化为节点图），把「多智能体黑盒」变为产品自带的可观测能力
- **模式智能推荐**：基于历史任务的成功率与成本数据，在任务发起时推荐 safe / auto 模式与预算档位（保留用户最终决策权，D.1 兼容）
- **运维闭环补齐（P2 顺延并入）**：任务取消与僵尸清理、镜像预热、Docker 桌面引导、插件配置页、自动发布流水线等
- **体验补全**：设置页重设计、深色模式、模型路由明细

### 1.2 关键指标（KPI）

| 指标 | 当前 v0.4 | v0.5 目标 | 衡量方式 |
|------|-----------|-----------|----------|
| 综合评分 | 80 / 100 | 85 / 100 | 九维度加权 |
| 陌生技术栈任务首过率 | 未量化 | 引入 Researcher 后提升 ≥15% | 标准任务集 A/B（scripts/ab_triage_eval.py 模式） |
| 多智能体过程可观测 | 执行日志文本 | 对话流图 + 面板全覆盖 | 功能验收 |
| 模式推荐采纳率 | 无 | 推荐准确率 ≥80% | 历史任务回放验证 |
| 测试用例数 | 500+ | 560+ | `pytest --collect-only` |
| 沙箱执行启动时间 | 未优化 | < 5s（镜像预热后） | 计时脚本 |

---

## 二、任务总览

### 2.1 按优先级统计

| 优先级 | 数量 | 说明 |
|--------|------|------|
| P0 必须做 | 4 项 | Researcher 降级版 + 触发器 + 注入链路；v0.5 版本底线 |
| P1 应该做 | 8 项 | 可视化三件套、智能推荐、高价值顺延项 |
| P2 可以做 | 7 项 | 体验与生态补全，时间充裕再做 |
| **合计** | **19 项** | 全部为 v0.4 P2 顺延承接 + v0.5 新增 |

### 2.2 模块划分

| 模块 | 任务数 | 说明 |
|------|--------|------|
| M10 Researcher Agent | 5 | v0.5 核心新增（规格第 4 章） |
| M11 高级可视化与智能推荐 | 3 | v0.5 核心新增（规格 16.2/16.3） |
| M12 运维与生态（v0.4 P2 顺延承接） | 9 | M1-8/M2-6/M2-7/M3-3/M3-4/M5-4/M5-5/M6-2/M8-6 |
| M13 发布 | 2 | M7-5 流水线（P2）+ v0.5 发布物 |

---

## 三、详细任务清单

### M10 · Researcher Agent（规格第 4 章，v0.5 核心）

> 角色定位：可选前置角色，编程任务前按需激活，不属于固定团队编制；解决 LLM 知识过时 → 技术方案不可行问题。

| # | 任务 | 优先级 | 依赖 | 验收标准 |
|---|------|--------|------|----------|
| M10-1 | Researcher 角色骨架（`app/agents/researcher.py`）：结构化摘要输出（来源/版本/用法示例/坑点四段式） | P0 | 无 | 用户提供资料 → 输出结构化摘要；单测覆盖解析与校验 |
| M10-2 | 触发器接线（三条件量化，规格 4.5）：用户显式指定 / 主 LLM 评估 reason 标注陌生技术栈 / 同一模块修复失败 ≥2 轮系统建议（用户确认后激活） | P0 | M10-1 | 三触发路径各有测试；默认不自动激活（D.1：决策归用户） |
| M10-3 | 知识注入链路：摘要作为 System Prompt / RAG 上下文注入 Dev / Test Agent | P0 | M10-1 | 注入后 Dev/Test 提示词含研究上下文；注入回归（不可信文本包裹，M7-6 同构） |
| M10-4 | 独立预算与缓存：research_budget（默认 20k，独立于任务总预算、计入全局日志）；缓存键 = 技术栈+API+版本三元组 | P0 | M10-1 | 预算超限熔断；相同查询命中缓存零调用 |
| M10-5 | 联网调研通道（搜索/文档抓取 API） | P2 | M10-4, 外部工具 | 可配置开关；联网失败回退资料注入模式 |

### M11 · 高级可视化与智能推荐（规格 16.2/16.3）

| # | 任务 | 优先级 | 依赖 | 验收标准 |
|---|------|--------|------|----------|
| M11-1 | 实时监控面板升级（16.2）：阶段耗时分布、token 消耗曲线、模块状态全景（client.html + 插件同步） | P0 | 无（消费 v0.4 SSE 事件流） | 面板数据与 logs/ 审计口径一致 |
| M11-2 | Agent 对话流图（16.3）：PM→双评审→收敛→模块开发 的多智能体消息流可视化为节点图（纯前端 SVG，不引框架） | P1 | M11-1 | 演示与真实任务均可渲染；节点可展开查看消息原文 |
| M11-3 | 模式智能推荐：按项目历史（sessions/ 成功率与 token 成本）在发起时推荐 safe/auto 与预算档位，展示推荐理由 | P1 | 历史数据（v0.4 已落盘） | 推荐理由可解释（引用历史数据）；用户可覆盖（D.1） |

### M12 · 运维与生态（v0.4 P2 顺延项全部承接）

| # | 任务 | 优先级 | 依赖 | 验收标准 |
|---|------|--------|------|----------|
| M12-1 | 任务取消与僵尸清理（原 M8-6）：`DELETE /api/tasks/{id}` 取消排队/执行中任务；服务重启后僵尸任务正确标记 | P1 | v0.4 异步任务 API | 取消后线程释放（协作式取消）；重启后状态正确 |
| M12-2 | 镜像缓存与预热（原 M2-6）：任务开始前预热 python/node 镜像，执行启动 <5s | P1 | v0.4 M2-5 | 二次执行启动时间达标 |
| M12-3 | 桌面端 Docker 检测与引导（原 M2-7）：未安装时给出指引 + 自动降级进程模式 | P2 | v0.4 M2-2 | 未装 Docker 的机器桌面端可用且提示明确 |
| M12-4 | 插件配置页（原 M1-8）：VS Code 设置页配置 serverUrl/serverDir/预算/默认模型 | P2 | v0.4 M1-1 | 设置变更即时生效 |
| M12-5 | 路由 A/B 测试框架产品化（原 M3-3）：把 `scripts/ab_triage_eval.py` 扩展为可配置诉求集 + 报告落盘 | P1 | M9-4 框架（已建） | 标准集可自定义；报告含 KPI 判定 |
| M12-6 | 用户自定义路由策略（原 M3-4）：各阶段模型档位与阈值可配置 | P2 | M3-1 | 配置后路由按自定义规则分发 |
| M12-7 | client.html 设置页重设计（原 M5-4）：分类设置（模型/预算/安全/外观） | P2 | M5-1 组件库 | 设置分组清晰，保存后生效 |
| M12-8 | 深色/浅色主题切换（原 M5-5）：token 双套 + 跟随系统 | P2 | M5-1 tokens | 两套主题下全部页面无对比度问题 |
| M12-9 | 模型路由明细（原 M6-2）：每次调用显示实际档位与旗舰对比成本 | P2 | M3-2 | 看板可按调用展开档位与假设成本 |
| M12-10 | 插件 E2E 测试轻量方案：扩展宿主外对 PanelProvider 消息协议做契约测试 | P2 | v0.4 M1 系列 | 消息协议契约测试全绿 |

### M13 · 发布

| # | 任务 | 优先级 | 依赖 | 验收标准 |
|---|------|--------|------|----------|
| M13-1 | GitHub Actions 发布流水线（原 M7-5）：pytest → PyInstaller 构建 → Release 产物 | P2 | 全部 | Actions 全绿，Release 附 EXE |
| M13-2 | v0.5 发布物：CHANGELOG、PyInstaller 重构建（≤80MB / 冷启动 <2s 复验）、真实 A/B 报告归档 | P0 | 上述全部 | Done 定义全过 |

---

## 四、里程碑

| 里程碑 | 内容 | 准入 |
|--------|------|------|
| V0 基础 | M10-1/2/3/4（Researcher 降级版闭环） | 注入链路测试全绿；触发器三路径可用 |
| V1 可视化 | M11-1/2 | 面板与流图在真实任务中渲染正确 |
| V2 运维与推荐 | M12-1/2/5、M11-3 | 取消语义正确；推荐可解释 |
| V3 发布 | M10-5（视外部能力）、M12 其余、M13 | Done 定义全过 |

---

## 五、依赖关系

```
M10-1 骨架 ── M10-2 触发器 ── M10-4 预算与缓存 ── M10-5 联网（P2）
                └────────── M10-3 注入链路
M11-1 监控面板 ── M11-2 对话流图
M11-3 智能推荐（独立，消费 v0.4 历史数据）
M12-1 取消（独立）／ M12-2 预热（依赖 M2-5 ✓）／ 其余独立小项
M13 发布 ── 依赖全部
```

---

## 六、风险与注意事项

| 风险 | 应对 |
|------|------|
| Researcher 调研结果质量不可控 | 标注来源与版本；注入前用户确认（规格 19 章既有应对） |
| 联网调研依赖外部工具调用（v0.5 未必就绪） | 先交付「资料注入」降级版，联网通道独立开关灰度 |
| 对话流图前端复杂度失控 | 纯 SVG + 既有 token，不引框架；先只读渲染后交互 |
| 模式推荐数据冷启动 | 无历史时推荐缺省 safe 并说明理由 |
| P2 顺延项批量合入引入回归 | 每项独立开关/独立提交，全量回归护栏不变 |

---

## 七、Done 的定义

- [ ] 全部 P0（4 项）完成并通过单元测试
- [ ] Researcher 降级版闭环可用：触发 → 摘要 → 注入 → Dev/Test 消费，预算与缓存生效
- [ ] 可视化面板与对话流图在真实任务中渲染正确（手动验收）
- [ ] 模式推荐可解释且用户可覆盖
- [ ] P2 顺延项按上表完成或明确再顺延（M12-10 可顺延）
- [ ] 全量测试基线（500+）无回退，新增 ≥60 用例
- [ ] CHANGELOG 与升级说明就绪；PyInstaller 产物复验通过
