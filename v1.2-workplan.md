# token-burner v1.2 工作计划（行业基准 · SWE-bench Lite / AI 实现 / 用户验收）

> 依据：2026-09-06 市场对标讨论——SWE-bench 家族是自主工程赛道唯一接近
> 公认的标尺（Devin/OpenHands/Claude Code 同台语言）；token-burner 现有
> bench_v1（greenfield 模块通过率）是自建方法论，缺行业可比性。
> 主题：**拿到与 Devin/OpenHands 同台的资格**——在 SWE-bench Lite 上
> 产出可公开、可复现的分数。
> 基线：v1.0（bench_v1 试点 62%、全量基准口径见 logs/bench_v1/full*）。

***

## 0. 关键认知（先说清楚难度）

1. **SWE-bench 测的是"修真实仓库的 bug"**,与 token-burner 的
   greenfield 生成是两种任务形态——需要给管线新增 **repo-patch 模式**
   （克隆仓库至 base commit → issue 文本作为需求 → dev 产出补丁 →
   跑仓库自带测试），这是本计划的核心工程量,也是诚实的部分。
2. **评分必须用官方 harness**（SWE-bench 官方 docker 评测容器,
   FAIL_TO_PASS + PASS_TO_PASS 双口径）,自造评分没有公信力。
3. **成本**:Lite 全量 300 实例 × 100-300k tokens ≈ 30-90M tokens——
   首战跑 **50 实例分层子集**,方法论公开;分数有底气再上全量。

***

## S1 · repo-patch 模式（P0）：管线支持"修已有仓库"

**改动点**：

- `Pipeline` 新增任务形态 `repo_fix`：输入 = 仓库 URL + base commit +
  issue 文本；跳过 拆分/spec 确认（issue 即 spec）,单模块直通 dev 循环；
- `LocalExecutor`/`DockerExecutor` 新增 repo 工作区：在 base commit 上
  应用 dev 产出的 diff（`git apply` 校验）,而非生成全新文件树；
- write_code/write_tests 提示词新增 repo 变体（现有代码上下文注入,
  强调最小改动、禁止无关重构）；
- 门禁链复用（语法/静态/危险分级不变）,链接门禁与接口门禁对 repo 模式
  关闭（无 interfaces.json 契约,验收以仓库测试为准——诚实边界,写入文档）；
- 预算与修复轮沿用现有护栏。

**验收**：

- [ ] 手工构造 3 个迷你仓库 issue 场景（含 1 个多文件）→ 管线产出可
  apply 的 diff 且仓库测试转绿
- [ ] 全量回归绿（新增 ≥10 用例）;greenfield 主路径行为零变化

***

## S2 · Lite 子集首战（P0）：可公开的第一个分数

**改动点**：

- 子集构造：Lite 300 例中按仓库/难度分层抽 50 例（抽样脚本 + 清单
  入库,可复现）；剔除依赖重度 web 框架运行时的实例（沙箱限制,如实记录）；
- 评测：官方 harness docker 跑分,输出 resolved / unresolved / error
  三态 + 逐实例成本（tokens/耗时）；
- 报告：`logs/swebench/` 归档,方法论（模型组合、子集抽样、提示词、
  失败分类）成文公开——**对标 Devin 公开 78%/14-15% 的口径,同时也公布
  自己的失败分类**,这与 bench_v1 的取证文化一致。

**验收**：

- [ ] 50 实例完整跑通,harness 分数 + 成本报告归档
- [ ] 失败实例逐个人工归因（理解错题/补丁错/环境错三分类）
- [ ] README/评审文档新增「SWE-bench Lite (50)」口径段落

***

## S3 · 升级与公开（P1）

- 分数可看 → 扩到 Lite 全量 300;再进一步上 **SWE-bench Verified**
  （人工核验标签,当前更受认可）；
- 提交官方 leaderboard（可选,需公开可复现配置）；
- 评审报告竞品矩阵新增同台列(Devin/OpenHands/本产品)。

**验收**：

- [ ] Verified 100 实例子集分数归档（预算允许时）
- [ ] 竞品矩阵更新

***

## 风险清单

| 风险 | 应对 |
| --- | --- |
| repo-patch 模式工程量超预期 | S1 只做最小闭环(单文件补丁先行),多文件作为 stretch |
| 沙箱跑不动重型仓库测试 | 抽样时排除;如实记录;Docker 资源配额上调选项 |
| 分数难看(首战完全可能 <30%) | 方法论+失败分类同权重公开——「诚实的新玩家首战」本身是差异化叙事;对照 Devin 首战 13.86% 的历史 |
| token 成本失控 | 50 实例硬上限 + 单实例预算闸门沿用现有护栏 |

***

## Done 定义（v1.2）

- [ ] repo-patch 模式合入,greenfield 主路径零回归
- [ ] SWE-bench Lite 50 实例官方 harness 分数 + 方法论文档公开
- [ ] 失败分类归因报告归档
- [ ] 全量回归基线 1035 项无回退
