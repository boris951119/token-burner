# token-burner v0.4 Alpha · 任务清单与优先级

> 版本定位：核心能力补全 —— 补齐 IDE 集成、容器级沙箱、服务端并发、意图双模式四大短板，让产品从「能用」进化到「好用」
> 规划周期：3 个月（约 12 周）
> 目标综合分：72 → 80
> 前置基线：v0.3.1 全量实现（472+ 测试用例）

---

## 一、版本目标

### 1.1 核心目标
- **IDE 集成**：发布 VS Code 插件，让开发者不用离开编辑器就能使用多智能体编码能力
- **安全隔离**：从进程级升级到容器级沙箱；补齐提示词注入面防护（需求数据边界包裹），达到企业客户可接受的安全标准
- **并发架构**：拆除服务端全局串行锁，落地「任务级隔离 + 项目级锁 + 异步任务 API」，支撑多任务并行与插件实时进度
- **成本优化**：引入智能模型路由和上下文缓存，同等质量下节省 30%+ Token
- **意图双模式**：诉求先经轻量模型快判（System-1 快速反应式），简单意图直达响应、不进完整评估；低置信或命中边界信号时升级完整评估（System-2 深思熟虑）；意图域补全「闲聊/无意义」，让 declined 成为真实意图出口
- **体验提升**：桌面端 UI 改版，交互流程和视觉质感全面升级

### 1.2 关键指标（KPI）

| 指标 | 当前 v0.3.1 | v0.4 目标 | 衡量方式 |
|------|------------|-----------|----------|
| 综合评分 | 72 / 100 | 80 / 100 | 九维度加权 |
| IDE 集成 | 35 分 | 75 分 | 功能覆盖度评估 |
| 安全性 | 75 分 | 85 分 | 沙箱隔离强度评估 |
| Token 节省率 | 0% | 30%+ | 相同任务对比测试 |
| 意图双模式 | 无（单模式全量评估） | 快判承接 ≥60% 诉求、误判率 <5% | 标准诉求集 A/B 对比（M9-4） |
| 测试用例数 | 472+ | 600+ | `pytest --collect-only` |
| 桌面端加载时间 | - | < 2s | 冷启动计时 |
| 沙箱启动时间 | - | < 5s | Docker 容器启动计时 |
| 并发任务能力 | 1 路（全局串行） | ≥ 4 路并行 | 压测脚本并发提交验证 |
| 任务提交响应 | 数分钟（同步阻塞） | < 200ms | 异步 API 返回 task_id 计时 |

---

## 二、任务总览

### 2.1 按优先级统计

| 优先级 | 数量 | 预估工时 | 说明 |
|--------|------|----------|------|
| P0 必须做 | 18 项 | 约 66 人天 | 版本交付底线，缺一个就不叫 v0.4 |
| P1 应该做 | 18 项 | 约 52 人天 | 有了显著提升，没有也能发布 |
| P2 可以做 | 11 项 | 约 25 人天 | 时间充裕就做，否则顺延 |
| **合计** | **47 项** | **约 143 人天** | 3 人 × 12 周 ≈ 180 人天，留约 21% 缓冲 |

### 2.2 按模块统计

| 模块 | 任务数 | P0 | P1 | P2 | 预估工时 |
|------|--------|----|----|----|----------|
| M1 VS Code 插件 | 8 | 4 | 3 | 1 | 35 人天 |
| M2 Docker 沙箱 | 7 | 3 | 2 | 2 | 25 人天 |
| M3 智能模型路由 | 4 | 2 | 1 | 1 | 12 人天 |
| M4 上下文缓存 | 4 | 1 | 2 | 1 | 10 人天 |
| M5 桌面端 UI 改版 | 5 | 1 | 2 | 2 | 14 人天 |
| M6 成本优化看板 | 2 | 0 | 1 | 1 | 4 人天 |
| M7 工程质量 & 发布 | 6 | 2 | 2 | 2 | 14 人天 |
| M8 并发架构改造 | 6 | 3 | 2 | 1 | 21 人天 |
| M9 双模式意图识别 | 5 | 2 | 3 | 0 | 8 人天 |

---

## 三、详细任务清单

### M1 · VS Code 插件

> 目标：在 VS Code 侧边栏中集成 token-burner，支持发起任务、查看进度、应用代码、查看成本

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M1-1 | 插件脚手架与架构设计 | P0 | 3 人天 | 无 | 插件可安装激活，侧边栏面板正常显示，与后端 API 通信链路打通 |
| M1-2 | 任务发起面板 | P0 | 5 人天 | M1-1 | 可在侧边栏输入需求、选择模型、选择模式（safe/auto），点击开始任务 |
| M1-3 | 实时进度与日志展示 | P0 | 6 人天 | M1-2, M8-3 | 任务执行中显示当前阶段、进度条、实时日志，支持展开/收起 |
| M1-4 | 生成代码预览与应用 | P0 | 7 人天 | M1-3 | 生成的代码可在编辑器中预览 diff，用户可选择「应用全部」或「逐文件应用」 |
| M1-5 | 成本统计侧边栏 | P1 | 4 人天 | M1-3 | 显示当前任务 Token 消耗、按模型/环节分布、预算剩余 |
| M1-6 | 历史任务列表 | P1 | 4 人天 | M1-1 | 侧边栏展示历史项目列表，可点击查看详情或恢复任务 |
| M1-7 | 任务中断恢复交互 | P1 | 3 人天 | M1-3, M1-6 | 中断的任务在列表中有标识，点击可一键恢复续跑 |
| M1-8 | 配置页（模型/密钥/预算） | P2 | 3 人天 | M1-1 | VS Code 设置页面中配置 API Key、模型列表、默认预算等参数 |

**M1 关键设计决策：**
- 后端复用现有 FastAPI Server（`app/server.py`），插件通过「异步任务 API 提交 + 轮询/SSE」（M8-3/M8-4）获取进度，不依赖同步长连接
- 代码应用走 VS Code Workspace Edit API，确保 Undo/Redo 正常
- 插件优先支持桌面端的本地 Server，后续再扩展远程 Server

---

### M2 · Docker 沙箱

> 目标：执行环境从进程级升级到容器级，提供文件系统隔离、资源配额、网络控制

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M2-1 | DockerExecutor 架构设计 | P0 | 3 人天 | 无 | 完成 Executor 接口的 Docker 实现方案设计，含镜像策略和生命周期管理 |
| M2-2 | 基础 Docker 执行器 | P0 | 6 人天 | M2-1 | 代码可在 Docker 容器中执行，输入输出通过卷挂载传递，超时正常熔断 |
| M2-3 | 安全策略（文件/网络/用户） | P0 | 5 人天 | M2-2 | 容器内只读文件系统 + 指定目录可写、无网络（可配置）、非 root 用户运行 |
| M2-4 | 资源配额（CPU/内存/磁盘） | P1 | 4 人天 | M2-2 | 每个执行容器有 CPU、内存、磁盘空间限制，超限自动终止 |
| M2-5 | 多语言基础镜像 | P1 | 3 人天 | M2-2 | Python 镜像 + Node.js 镜像（为 v0.5 TS 支持做准备），镜像体积 < 500MB |
| M2-6 | 镜像缓存与预热 | P2 | 2 人天 | M2-5 | 首次拉取后本地缓存，任务开始前预热容器，执行启动时间 < 5s |
| M2-7 | 桌面端 Docker 检测与引导 | P2 | 2 人天 | M2-2 | 桌面端检测 Docker 是否安装，未安装时给出安装指引和降级方案（进程模式） |

**M2 关键设计决策：**
- 继承现有 `Executor` 抽象接口（`app/execution/executor.py`），新增 `DockerExecutor`
- 安全模式（safe）不需要 Docker，只有自动验证模式（auto）才使用容器
- 桌面端检测不到 Docker 时自动降级到 LocalExecutor（进程模式），不阻塞使用
- 文件传输策略：代码写入临时目录 → 挂载到容器 → 执行 → 读取输出 → 清理

---

### M3 · 智能模型路由

> 目标：根据任务难度和阶段自动选择模型，简单任务用廉价模型，复杂任务才调用高端模型，在不降低质量的前提下节省 Token

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M3-1 | 模型分层与路由规则设计 | P0 | 2 人天 | 无 | 定义「旗舰/主力/轻量」三档模型分类，以及各阶段的默认路由规则 |
| M3-2 | 评估阶段动态选模 | P0 | 4 人天 | M3-1 | 难度评估后自动调整后续模型：简单任务降档，高难度保持旗舰模型 |
| M3-3 | 路由效果 A/B 测试框架 | P1 | 3 人天 | M3-2 | 可对同一任务用不同策略跑对比，输出质量/成本双维度数据 |
| M3-4 | 用户自定义路由策略 | P2 | 3 人天 | M3-1 | 高级用户可配置各阶段使用的模型档位和阈值 |

**M3 关键设计决策：**
- 三档模型：旗舰（GPT-4o / Claude Opus）、主力（GPT-4o-mini / Claude Sonnet / DeepSeek）、轻量（DeepSeek-Lite 等）
- 路由规则：难度 1-3 全轻量、4-6 主力+轻量混合、7-10 全旗舰
- 方案讨论阶段：主 LLM 用主力档，副 LLM 评审用轻量档（评审不需要最强模型）
- 保守策略：宁可多花一点 Token 也不能明显降低质量，质量底线是 v0.3.1 水平

---

### M4 · 上下文缓存

> 目标：缓存跨任务可复用的上下文（文件 embedding、公共模块、提示模板），减少重复 Token 消耗

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M4-1 | Embedding 缓存层 | P0 | 3 人天 | 无 | 对相同文件内容的 embedding 结果做本地缓存，命中时直接返回不调用 API |
| M4-2 | 循环检测论点库持久化 | P1 | 3 人天 | M4-1 | 历史任务的论点指纹可复用，新项目遇到相似论点更快触发循环检测 |
| M4-3 | 公共模块（_shared）缓存 | P1 | 2 人天 | M4-1 | _shared 目录的代码和接口信息缓存，跨模块开发时不重复读入上下文 |
| M4-4 | 缓存命中率统计 | P2 | 2 人天 | M4-1 | 成本看板中显示缓存命中率和节省的 Token 数量 |

**M4 关键设计决策：**
- 缓存键：文件内容哈希 + 模型名称 + embedding 维度
- 缓存存储：SQLite 本地数据库（轻量、单文件、易于清理）
- 缓存失效：手动清理按钮 + 按时间过期（默认 7 天）
- 不缓存任何用户私有数据（如 API Key、任务内容摘要），只缓存 embedding 向量

---

### M5 · 桌面端 UI 改版

> 目标：提升桌面客户端的视觉质感和交互流畅度，让产品看起来「更像正经商业软件」

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M5-1 | 设计规范与组件库 | P0 | 3 人天 | 无 | 确定配色、字体、间距规范，抽出通用组件（按钮、卡片、进度条等） |
| M5-2 | 首页/项目列表页重设计 | P1 | 3 人天 | M5-1 | 新的启动页设计：新建项目 + 历史项目列表 + 快速模板 |
| M5-3 | 任务执行页重设计 | P1 | 4 人天 | M5-1 | 更清晰的阶段进度展示、实时日志区、成本面板、操作按钮区 |
| M5-4 | 设置页重设计 | P2 | 2 人天 | M5-1 | 分类设置项（模型/预算/安全/外观），更清晰的配置引导 |
| M5-5 | 深色模式 | P2 | 2 人天 | M5-1 | 支持深色/浅色主题切换，跟随系统或手动选择 |

**M5 关键设计决策：**
- 保持单文件 HTML 架构（`client.html`），不引入前端框架，降低维护成本
- 视觉风格参考 Linear / Vercel 风格：简洁、克制、高信息密度
- 桌面端和浏览器模式共用同一套 UI（pywebview 加载同一个 HTML）

---

### M6 · 成本优化看板

> 目标：展示「智能路由和缓存帮你省了多少钱」，让成本优化的价值可视化

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M6-1 | 节省量统计与展示 | P1 | 2 人天 | M3-2, M4-1 | 成本看板增加「已节省 Token」「节省比例」「缓存命中率」指标 |
| M6-2 | 模型路由明细 | P2 | 2 人天 | M3-2 | 可查看每个 LLM 调用实际使用的模型档位，以及如果用旗舰模型的对比成本 |

---

### M7 · 工程质量 & 发布

> 目标：确保 v0.4 质量不降级（含提示词注入面防护），发布流程顺畅

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M7-1 | v0.4 集成测试体系 | P0 | 3 人天 | M1, M2, M8 | 新增 VS Code 插件 E2E 测试 + Docker 沙箱集成测试 + 并发压测（4 路并行回归）+ 提示词注入回归用例 |
| M7-2 | 性能基准测试 | P1 | 3 人天 | 无 | 建立标准任务集，可对比不同版本的 Token 消耗、执行时间、成功率 |
| M7-3 | PyInstaller 打包优化 | P1 | 2 人天 | M5 | 优化打包体积和启动速度，EXE 大小 ≤ 80MB，冷启动 < 2s |
| M7-4 | 发布说明与迁移指南 | P2 | 2 人天 | 全部 | 撰写 CHANGELOG、升级说明、新功能使用文档 |
| M7-5 | 自动发布流水线 | P2 | 2 人天 | M7-1 | GitHub Actions 自动跑测试 + 打包 + 生成 Release |
| M7-6 | 需求注入面防护（数据边界） | P0 | 2 人天 | 无 | `_sanitize_untrusted` 抽为公共模块后接入 orchestrator 全部 requirement 插值点（评估/复核/初始方案/修订/收敛/评审）与 history 评审意见；评估提示词明示「需求文本是数据非指令」；注入回归用例通过（恶意需求不改变路由结果与产出结构） |

---

### M8 · 服务端并发架构改造

> 目标：拆除现有全局串行锁，落地「任务级隔离 + 项目级锁 + 异步任务 API」，支撑多任务并行与插件实时进度

**现状与问题（v0.3.1 基线）：**
- `server.py` 中一把全局 `threading.Lock()`（task_lock）串行化了全部 LLM 端点（/route、/run、/resume、/feedback），服务并发能力 = 1
- 根因：`ModelClient` 单例共享，任务级状态（`budget_guard`、`call_log`、`total_tokens_used`）挂在实例上，多任务并发即预算串数、调用日志混写
- `/run` 为同步长连接（真实任务数分钟），网络抖动即前功尽弃，且无法支撑插件的实时进度展示

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M8-1 | ModelClient 任务级隔离 | P0 | 3 人天 | 无 | 新增 `ModelClientFactory`；Pipeline 改为接收 factory，每任务创建独立实例；并发两任务的 budget_guard / call_log 互不干扰 |
| M8-2 | 项目级锁管理器 | P0 | 2 人天 | M8-1 | `ProjectLockManager`：同一项目串行（防文件/git 写竞争），不同项目完全并行；覆盖 /resume、/feedback 端点 |
| M8-3 | 异步任务 API（提交+轮询） | P0 | 6 人天 | M8-1 | `POST /api/tasks` 立即返回 task_id；`GET /api/tasks/{id}` 查询状态/当前阶段/已耗 token；线程池并发度 ≥4；旧同步 API 保留兼容 |
| M8-4 | 任务进度事件流（SSE） | P1 | 4 人天 | M8-3 | `GET /api/tasks/{id}/events` 实时推送阶段切换/日志/token 更新事件；断线自动重连 |
| M8-5 | 全局 LLM 限流器 | P1 | 3 人天 | M8-1 | 令牌桶控制对同一供应商的并发请求；超限排队而非直接报错；429 退避复用既有重试机制（9 章） |
| M8-6 | 任务取消与僵尸清理 | P2 | 3 人天 | M8-3 | `DELETE /api/tasks/{id}` 可取消排队/执行中任务并释放资源；服务重启后僵尸任务正确标记与清理 |

**M8 关键设计决策：**
- **不动 Pipeline 内部编排**：只改「谁持有 ModelClient」——每任务一个实例，预算闸门与审计日志天然隔离，这是改动最小、收益最大的切入点
- **锁粒度从全局降到项目**：文件写与 git 提交的竞争只发生在项目目录内，项目级互斥即可覆盖，无需全局锁
- **异步 API 是 VS Code 插件的硬前置**：M1-3 实时进度依赖 task_id 轮询/SSE，同步长连接在插件侧不可行，M8-3 必须在 Sprint 2 前半段落
- **任务状态落盘**：TaskState 写入项目 `sessions/`（与现有断点续跑同构），服务重启后任务可查询、可恢复，不丢现场
- **进程级隔离留给 v1.0**：Worker 进程 + 任务队列方案配合 Docker 沙箱与多用户场景再做，v0.4 不引入分布式复杂度

---

### M9 · 双模式意图识别（System-1 / System-2）

> 目标：诉求先经轻量模型快判（System-1 快速反应式），简单意图直接响应、不进完整评估；低置信或命中边界信号时升级完整评估（System-2 深思熟虑）；意图域补全「闲聊/无意义」，使 declined 成为真实意图出口

**现状与问题（v0.3.1 基线）：**
- 所有诉求无差别走一次完整评估（`TASK_ASSESSMENT`，主模型 json_mode），哪怕「你好」也要付一次旗舰模型调用的 token 与延迟
- 「快」的部分全是确定性程序逻辑（执行类关键词正则、确认词表、阈值分发），是护栏而非快判——不存在 LLM 级快速反应式通道
- 复核（`_recheck`）由关键词触发，而非置信度触发的快→慢升级
- `task_type` 取值域无「闲聊/无意义」类，declined 仅在「评估解析失败 → 保守视作编程 → 用户拒绝确认」链上出现，不是主动意图判定

| # | 任务 | 优先级 | 预估工时 | 依赖 | 验收标准 |
|---|------|--------|----------|------|----------|
| M9-1 | 意图域扩展与快判契约设计 | P0 | 1 人天 | 无 | 新增快判意图枚举（编程｜研究/分析｜基础｜闲聊｜无意义）与 `{intent, confidence, reason}` JSON 契约冻结（含非法值处理规则）；System-2 的 task_type 三值取值域不变，既有校验不破坏 |
| M9-2 | FastTriage 快判器实现 | P0 | 2 人天 | M9-1 | 快判接入 `TaskRouter.route` 前置；确定性升级规则：置信度 < 阈值 / 意图为编程·研究分析 / 执行类关键词命中 → 升级 System-2；高置信闲聊·无意义 → declined；高置信基础 → direct_answer；快判解析失败静默降级 System-2；`fast_triage_enabled` 关闭时路由行为与 v0.3.1 完全一致（回归通过） |
| M9-3 | declined 出口语义升级 | P1 | 2 人天 | M9-2 | 闲聊/无意义 → declined（附友好文案字段）；CLI / API / 桌面三端展示适配；client.html 同步更新 |
| M9-4 | 快慢双模式 A/B 验证 | P1 | 2 人天 | M9-2 | 标准诉求集（闲聊 / 基础问答 / 简单编程 / 复杂编程）对比单模式与双模式：token 消耗、误判率、端到端延迟三维度报告，支撑 KPI（快判承接 ≥60%、误判率 <5%）验收 |
| M9-5 | 评估降档联动 | P1 | 1 人天 | M9-2, M3-1 | System-2 评估调用降档至主力档（评估是轻量分类任务，无需旗舰）；复用 M3-1 三档配置，不新增模型管理逻辑 |

**M9 关键设计决策：**
- **档位切换是确定性规则，决策归大模型（D.1 兼容）**：程序只做「置信度阈值 / 意图类别 / 边界关键词」的确定性判定来决定快→慢升级；意图与置信度本身由 LLM 输出，程序不越权改写
- **快判只承接最便宜的出口**：闲聊/无意义 → declined；高置信「基础」→ direct_answer；编程与研究/分析一律升级 System-2——编程任务需要 difficulty_score 与 estimated_files 做模块化判定与团队组建，快判不提供这些字段
- **失败方向单一**：快判任何异常（解析失败 / 超时 / 低置信）都只降级到 System-2，不新增失败模式；System-2 失败沿用既有 fallback 链（保守视作编程 + 用户确认）
- **开关可回退**：`fast_triage_enabled` 缺省关闭合入（行为等同 v0.3.1），标准诉求集验证通过后再灰度开启；快判模型缺省取预设列表中的轻量档，与 M3-1 分层对齐
- **token 计量与审计不断裂**：快判调用照常计入 BudgetGuard 与 call_log（快判本身也是成本，A/B 报告须体现）
- **新增配置项**：`fast_triage_enabled`（缺省 False）、`fast_triage_model`（缺省预设轻量档）、`fast_triage_confidence_threshold`（缺省 0.8，宁升级勿误判）

---

## 四、里程碑与排期

### 4.1 三周冲刺安排（共 12 周）

| 冲刺 | 周次 | 核心目标 | 重点任务 |
|------|------|----------|----------|
| Sprint 1 | 第 1-3 周 | 骨架搭建 + 并发与意图地基 | M8-1/2 ModelClient 隔离与项目锁、M1-1 插件脚手架、M2-1 沙箱设计、M3-1 路由设计、M9-1/2 快判契约与快判器、M5-1 设计规范 |
| Sprint 2 | 第 4-6 周 | P0 核心功能 | M8-3 异步任务 API（前半段落，M1-3 依赖）、M1-2/3/4 任务流、M2-2/3 Docker 执行与安全、M3-2 动态选模、M7-6 需求注入面防护 |
| Sprint 3 | 第 7-9 周 | P1 功能完善 | M8-4 SSE 事件流、M8-5 LLM 限流器、M1-5/6/7 成本与历史、M2-4/5 资源与多镜像、M4-1/2 缓存、M5-2/3 UI、M9-3/4/5 意图出口与 A/B 验证 |
| Sprint 4 | 第 10-12 周 | 测试与发布 | M8-6 任务取消（时间允许）、M7-1 集成与并发压测、M7-2 性能基准、M7-3 打包优化、Bug 修复、发布 |

### 4.2 关键里程碑

| 里程碑 | 时间点 | 交付物 | 准入标准 |
|--------|--------|--------|----------|
| M0 启动 | 第 1 周初 | v0.4 任务清单确认 | 全部 P0 任务已拆解到人、工时确认 |
| M1 骨架完成 | 第 3 周末 | 可运行的最小闭环 | 插件能发起任务、Docker 能跑 Hello World、M8-1/2 合入（两任务并行互不干扰） |
| M2 P0 完成 | 第 6 周末 | 核心功能全部可用 | 全部 P0 任务完成并通过基本测试（含异步任务 API 提交/查询闭环） |
| M3 功能冻结 | 第 9 周末 | 全功能可用 | P1 任务全部完成，进入测试阶段 |
| M4 RC 版本 | 第 11 周中 | v0.4-rc1 | 集成测试全部通过，无 P0/P1 Bug |
| M5 正式发布 | 第 12 周末 | v0.4.0 | Release 打包完成，文档就绪 |

---

## 五、依赖关系图

```
M1-1 插件脚手架 ──┐
                   ├── M1-2 任务发起 ── M1-3 进度展示 ── M1-4 代码应用
M5-1 设计规范 ─────┘    │                │
                         │                ├── M1-5 成本统计
                         │                ├── M1-6 历史列表
                         │                └── M1-7 中断恢复
                         └── M1-8 配置页 (P2)

M2-1 架构设计 ── M2-2 基础执行器 ──┬── M2-3 安全策略
                                    ├── M2-4 资源配额 (P1)
                                    ├── M2-5 多语言镜像 (P1)
                                    └── M2-6 镜像预热 (P2)

M3-1 规则设计 ── M3-2 动态选模 ── M3-3 A/B测试 (P1) ── M3-4 自定义 (P2)
       └────────── M9-5 评估降档联动 (P1)

M9-1 意图域与快判契约 ── M9-2 快判器 ──┬── M9-3 declined 出口 (P1)
                                       └── M9-4 快慢 A/B 验证 (P1)

M4-1 Embedding缓存 ──┬── M4-2 论点库持久化 (P1)
                       ├── M4-3 公共模块缓存 (P1)
                       └── M4-4 命中率统计 (P2)

M5-1 设计规范 ──┬── M5-2 首页改版 (P1)
                 ├── M5-3 执行页改版 (P1)
                 ├── M5-4 设置页 (P2)
                 └── M5-5 深色模式 (P2)

M8-1 ModelClient隔离 ──┬── M8-2 项目级锁
                       ├── M8-3 异步任务API ──┬── M8-4 SSE事件流 (P1)
                       │                       ├── M8-6 任务取消 (P2)
                       │                       └── M1-3 插件进度展示（硬依赖）
                       └── M8-5 LLM限流器 (P1，可与 M8-3 并行开发)

M7-1 集成测试 依赖 M1, M2, M8 核心功能
M7-2 性能基准 可独立进行
M7-3 打包优化 依赖 M5 UI 改版
M7-6 需求注入面防护 独立可先行（建议 Sprint 2 与 M1-2 同期合入，插件上线前必须完成）
```

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| VS Code 插件调试困难，编辑器 API 踩坑多 | 高 | 延期 1-2 周 | Sprint 1 先做技术验证，必要时降低 M1 范围（砍掉 M1-7/8） |
| Docker 桌面端兼容性问题（Windows/Mac 差异大） | 中 | 桌面端沙箱体验差 | v0.4 优先保证服务端 Docker 可用，桌面端保留进程模式降级 |
| 智能路由导致代码质量下降 | 中 | 用户信任受损 | 做 A/B 对比测试，质量下降超过 5% 则回退路由策略 |
| 三人团队并行开发冲突多 | 中 | 效率降低 | 明确模块边界，接口先行，每周集成联调 |
| pywebview 在新版 UI 下有兼容性问题 | 低 | 桌面端显示异常 | 保留浏览器模式作为备选，UI 改版初期就做桌面端验证 |
| 拆除全局锁后暴露隐藏竞态（文件写入/日志混写） | 中 | 数据错乱、预算误判 | M8-1/2 Sprint 1 先行合入并补并发回归测试；BudgetGuard 补多线程场景单元测试 |
| 异步任务改造引入状态不一致（任务丢失/僵尸任务） | 中 | 用户任务卡死 | 任务状态落盘 sessions/，服务重启可恢复；Sprint 2 即做故障注入测试 |
| 并行度提升后触发 LLM 供应商限流（429） | 高 | 任务失败率上升 | M8-5 全局限流器排队而非报错；429 退避复用既有 9 章重试机制 |
| 快判误分类（编程被误直出 / 闲聊被误答） | 中 | 用户信任受损 | 升级规则保守（宁可升级勿误判，阈值缺省 0.8）；M9-4 标准诉求集回归；fast_triage_enabled 一键回退单模式 |
| 插件场景第三方文本注入需求面（issue/文档粘贴） | 中 | 提示词劫持、预算滥用 | M7-6 需求数据边界包裹全部插值点；M7-1 注入回归用例；执行侧 SafeExecutor 兜底不变 |

---

## 七、Done 的定义

v0.4 版本发布前，以下条件必须全部满足：

- [ ] 全部 P0 任务完成并通过单元测试
- [ ] VS Code 插件可在 Marketplace 上架（或至少可手动 sideload）
- [ ] Docker 沙箱在 Linux 环境下全功能可用
- [ ] 全部 472 个既有测试用例通过（无回归）
- [ ] 新增测试用例 ≥ 128 个（总数 ≥ 600）
- [ ] 性能基准测试：相同任务 Token 消耗下降 ≥ 20%
- [ ] 桌面端 EXE 打包成功，大小 ≤ 80MB
- [ ] CHANGELOG 和新功能文档撰写完成
- [ ] 至少 3 个真实任务的端到端验证通过（手动测试）
- [ ] 安全模式（safe）完全不受 Docker 依赖影响（向后兼容）
- [ ] 并发验收：4 路任务并行提交，各任务预算看板与调用日志正确隔离、无串数
- [ ] 异步 API：任务提交 < 200ms 返回 task_id，进度可查询，服务重启后任务状态可恢复
- [ ] 旧同步 API（/run 等）行为不变，客户端无感迁移（向后兼容）
- [ ] 意图双模式：快判开启时闲聊/基础类诉求不进入完整评估（M9-4 标准诉求集验证通过）；关闭时路由行为与 v0.3.1 完全一致（回归通过）
- [ ] 需求注入面防护：全部 requirement 插值点经数据边界包裹，注入回归用例通过（恶意需求不改变路由结果与产出结构）
