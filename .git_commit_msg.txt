feat: v0.5 Beta V2——运维与推荐（M12-1 任务取消与僵尸清理 + M11-3 模式智能推荐 + M12-5 A/B 框架产品化）：任务取消 DELETE /api/tasks/{id}——pending 立即取消（job 不执行）、running 协作式取消（取消旗标经 Pipeline 注入 BudgetGuard，ensure_allowed 检查点抛 TaskCancelledError 终止任务体，线程释放；取消与完成竞态用户意图优先）；recover_zombies() 服务启动清扫磁盘遗留 pending/running（服务重启僵尸标记 cancelled）；client.html「取消任务」按钮 + cancelled 终态友好提示；M11-3 模式智能推荐 GET /api/recommend——确定性统计历史项目（词袋相似度 ≥0.25：requirements.md / pipeline_state.json / logs/cost_report.json + interruption.md），输出 {mode, budget_tokens, reason}（成功率优先→平均成本→保守缺省；预算=成功样本均值×1.2 取整千位），理由引用数据、无 LLM 参与（D.1 可覆盖），client.html 评估阶段展示推荐（仅展示，模式下拉可覆盖）；M12-5 A/B 框架产品化——ab_triage_eval.py 新增 --cases 外置诉求集（JSON 数组 fail-fast 校验）+ 报告默认归档 logs/ab_reports/（--out 可指定，含 cases_file 来源标注）；+30 测试用例，全量 50 文件 779 用例回归绿
