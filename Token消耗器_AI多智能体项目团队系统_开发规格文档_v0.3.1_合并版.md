**AI 多智能体项目团队系统（Token 消耗器）**

**开发规格文档**

**版本：0.3.1（合并版）　　日期：2026-08-26**

状态：待评审定稿

目标：实现一个最小可行的多 LLM 协作系统，完成从用户需求到代码交付的核心闭环；

在可控 Token 成本内，实现可管理、可迭代、可回溯、可审计的项目交付。

本文档由 v0.2（实施规格）与 v0.3（演进提案）合并而成，自包含，无需再查阅旧版。

合并依据：《v0.2 → v0.3 版本对比分析报告》及已确认决策（沙箱不再排除；外部工具调用与可视化界面保留）。

总则：设计哲学（Design Principles）

P.1 核心决策哲学

**为 LLM 支付的成本应物尽其用：所有需要理解与权衡的「决策」交大模型，程序只承担确定性「校验」与底线护栏，不替代决策、不抢决策权。** 本原则贯穿全文，约束后续所有章节的实现方式。

D.1 决策权归属

- **大模型负责的决策**：难度评估、任务类型判定、技术方案设计、模块拆分、接口设计、评审打分、代码生成与修复、验收判断------所有需要理解模糊需求、权衡取舍的判断。

- **程序承担的两类兜底（不替代决策）**：

- **确定性校验**：可逐字精确判定、必须可重复且不随模型波动的事项------如接口是否登记、import 是否在白名单、引用集减合法集是否为 0。这类不交给大模型，避免其判断不稳定且空耗 token。

- **边界护栏**：当大模型决策落在不确定边界、或出现明确矛盾信号时（如判为基础任务但需求含「运行 .py / API / 脚本」），程序**不发回自判**，而是请求大模型**复核后重新决策**------最终拍板权始终在大模型。

D.2 判定标准（该交大模型还是程序）

- 判断是否需要理解语义、上下文或做权衡 → 交**大模型**。

- 判断是否靠逐字精确匹配、可枚举、要求结果稳定一致 → 交**程序**。

- 无法立即归类 → 默认交**大模型**决策，程序只做**复核触发**。

D.3 与本文档各节的约束关系

- 接口校验门禁（第 14 章）本质是**合规校验**，不是抢走接口设计决策------接口图纸仍由主 LLM 设计，门禁只守住「实现必须与图纸一致」这一底线。

- 任务类型判定（3.2 节）：主 LLM 正常判定；程序仅在边界处请求复核，不越权改写。

- 难度评估的「保守默认」（15.3 节）仅用于**极端硬回退**（多次解析仍失败、无任何结构化输出时）的兜底，不代表程序日常参与决策。

- 双模执行的模式分发（3.6 节）是**确定性配置执行**，不是决策；模式的选择权属于用户或显式规则。

- 沙箱高危操作拦截（3.6 节）属于**边界护栏**：拦截并明确告知，走「复核 / 升级给人」，不静默自决。

D.4 例外与边界

违背本原则的硬性限制（如安全问题、必须强制一致的底线）不在此列，程序在这些情形可强制拦截并明确告知，但仍走「复核 / 升级给人」而非静默自决。

P.2 四项工程原则

- **确定性优先**：程序负责确定性校验（AST / 静态分析），LLM 仅处理模糊决策与创造性任务，杜绝用 Token 做简单校验。

- **成本可控**：Token 消耗是可预测、可限制、可审计的工程指标，通过第 11 章六层护栏确保不失控。

- **渐进式信任**：系统能力随用户信任度动态调整，通过双模选择（3.6 节）平衡自动化效率与安全可控性。

- **文档即代码**：接口契约（interfaces.json）与模块文档是系统唯一真理源，所有代码变更必须同步更新，保障长期可维护性。

1\. 项目概述与定位

本系统是一个由多个大语言模型（LLM）组成的虚拟项目团队。主 LLM 担任项目经理，副 LLM 担任开发、测试等角色，共同完成用户提出的任务。

**产品定位**：以「Token 经济学」与「工程纪律」为核心竞争力的智能体运行时平台。区别于追求全 LLM 驱动的 Demo 级产品，本系统强调确定性校验、成本可控与渐进式信任，致力于成为可落地、可审计、可持续迭代的系统。

**MVP 核心流程**：需求识别 → 难度评估 → 任务类型判断 → 团队组建 → 方案讨论 → 生成代码 → 执行验证（双模）→ 修复交付。

2\. MVP 目标与范围

2.1 目标

- 跑通「需求 → 方案 → 代码 → 验证 → 反馈修复」的基本闭环。

- 验证多智能体协作的可行性，为后续扩展奠定基础。

- 提供命令行或简单 Web 界面与用户交互。

- 在可控 Token 成本（第 11 章）内完成上述闭环。

- 建立 Executor 抽象与安全审阅模式，为自动验证模式（沙箱）预留无重构接入路径（渐进式信任）。

- 提供基础 Token 成本看板，使消耗可观测、可审计。

2.2 范围（包含）

- 文本形式接收用户需求（暂不支持文件上传）。

- 主 LLM 进行需求解析、难度评估、任务类型判断（基础 / 研究·分析 / 编程，见 3.2）。

- 基础 / 研究·分析任务：主 LLM 直接产出结果（如文档、方案文本、分析报告），不强制组建团队。

- 编程任务强制组建固定团队：主 LLM + 开发副 LLM + 测试副 LLM（共 3 个，模型必须不同，可主动添加更多副 LLM）。

- 方案讨论：主 LLM 提出初步方案，开发 / 测试副 LLM 评审打分，主 LLM 汇总生成 spec.md（受轮数上限与循环检测约束，见第 11 章）。

- 代码生成：按需启用模块化拆分（第 12 章），受接口校验门禁约束（第 14 章）。

- **双模执行验证**：MVP 完整实现安全审阅模式（AST 静态检查 + LLM 逻辑审查 + 用户手动运行反馈）；建立 Executor 抽象接口，自动验证模式（沙箱）于 Alpha v0.4 落地（见 3.6）。

- **增强测试反馈**：生成可独立运行的测试文件，交付附带运行指令与预期输出；自动模式（v0.4）下真实执行并以结构化结果注入修复（见 3.7）。

- **基础 Token 看板**：任务级消耗、预算剩余与占用比例（高级可视化见第 16 章分阶段规划）。

- 项目持久化：每个项目独立目录，保存所有中间文件、接口地图与会话记录。

2.3 范围（不包含，路线图安排）

以下能力不再作为永久排除项，而是按路线图分版本承接：

  -----------------------------------------------------------------------------------------------------------------------------------------
  **能力**                               **安排**                         **说明**
  -------------------------------------- -------------------------------- -----------------------------------------------------------------
  沙箱自动验证模式                       Alpha v0.4                       本版不再排除沙箱；MVP 先做安全审阅模式并预留 Executor 接口

  外部 API / MCP 工具调用                路线图条目（Alpha 起逐步开放）   MVP 仅生成代码、不调用外部工具；Researcher 的联网调研依赖此能力

  Researcher Agent                       Beta v0.5                        规格先行定义于第 4 章，实现推迟

  高级可视化（对话流图、模式智能推荐）   Beta v0.5                        MVP 仅含基础 Token 看板

  本地知识库上传与分块检索               Release v1.0 之后                ---

  副 LLM 能力互联网调研与自动匹配        Release v1.0 之后                MVP 使用用户手动选择模型

  投票机制                               后续增强                         以「循环检测 + 主 LLM 收权」机制为基础演进

  动态调整团队成员、多人协作与权限管理   v1.0 之后                        ---
  -----------------------------------------------------------------------------------------------------------------------------------------

2.4 MVP 统一定义

**本文档的 MVP = v0.2 核心协作闭环（需求 → 方案 → 代码 → 反馈修复）+ 安全审阅模式完整实现 + Executor 抽象接口 + 基础 Token 看板。** 所有排期与验收以本定义为准，避免「MVP」一词在不同语境下的含义漂移。

3\. 核心功能描述

3.1 用户交互流程

1.  用户输入需求文本。

2.  系统显示主 LLM 的问候语和任务分析结果（难度、类型）。

3.  若为基础 / 研究·分析任务：主 LLM 直接生成结果并返回给用户。

4.  若为编程任务：系统要求用户选择主 LLM、开发副 LLM、测试副 LLM 的模型（预设列表，且必须不同），并选择执行模式（安全审阅模式为默认；自动验证模式于 v0.4 开放，任务级粒度）。

5.  系统创建项目目录，先通过成本总预算闸门检查（见 11.0；自动模式预算 ×2\~3，须 UI 明示并经用户确认）。

6.  经过方案讨论后生成 spec.md，显示给用户确认（确认环受收敛约束，见 11.5）。

7.  用户确认后，开发副 LLM 按模块生成代码文件（受接口校验门禁约束，见第 14 章）。

8.  执行验证：安全模式提示用户本地运行并反馈结果；自动模式（v0.4）由沙箱执行并自动捕获结果。

9.  若反馈报错或执行失败，开发副 LLM 修复（受最大轮数与门禁约束），循环直至用户确认成功、达成上限或手动结束。

3.2 难度评估与任务类型判断

主 LLM 根据用户需求，输出 JSON 格式：

> { \"difficulty_score\": 0-10, \"difficulty_level\": \"简单\|中等\|困难\",
>
> \"task_type\": \"基础\|研究/分析\|编程\", \"reason\": \"评估依据\" }

**任务类型三分类**：

  -----------------------------------------------------------------------------------------------------------------------------
  **task_type**   **典型示例**                             **处理路径**
  --------------- ---------------------------------------- --------------------------------------------------------------------
  基础            写一份方案文本、整理一段说明、格式转换   直接由主 LLM 产出结果返回（不建团队）

  研究 / 分析     竞品对比、数据解读、报告类非代码产出     主 LLM 独立完成；可按需简要拆解，不强制组建团队，控制成本

  编程            需要生成代码并本地运行                   进入完整团队协作流程（方案讨论 → spec → 模块化代码 → 验证 → 修复）
  -----------------------------------------------------------------------------------------------------------------------------

**路由规则**：

- 「基础」：主 LLM 直接产出返回。

- 「研究 / 分析」：主 LLM 独立产出；仅当难度 ≥ 8 时可选用一次「评审确认」辅助，仍不组建固定团队。

- 「编程」：默认进入团队流程。存在一处**简单编程节流**：当难度分 ≤ 3 时（与成本护栏联动的同一阈值 simple_threshold，见第 11 章），允许主 LLM 直接产出、跳过完整团队以节省 token。

- **阈值统一**：简单编程节流阈值 simple_threshold（默认 ≤3）与模块化启用阈值（默认 ≥5）取自同一 difficulty 尺度，统一收敛到同一配置集中管理，避免两处独立数值造成矛盾（如难度 4 的编程任务陷于「既不简单该节流、又不复杂该模块化」的空白带）。

- **空白带处理**：simple_threshold ≤ difficulty \< 模块化阈值的编程任务，按「标准团队流程」处理（完整讨论 + spec + 代码），只是不启用模块化拆分；不跳过也不降级，保证行为可预期。

**边界说明（避免三档模糊）**：

- 判断依据是「**产出是否为需在本地运行的代码**」：若是代码 → 编程；若是文本 / 报告型产物 → 基础或研究 / 分析。

- 「研究 / 分析」与「基础」的区别在**思考与内容深度**：研究 / 分析含多方比较、研判，基础为简单撰写 / 整理。

- 若主 LLM 难以判定，程序**不发回自判**，而以**边界护栏**方式请求主 LLM 复核（遵循总则 D 节）。

- 难度评估 JSON 解析失败时，按保守降级处理（见 15.3），视作编程任务并交用户确认。

3.3 团队模型配置

- 提供预设模型列表：gpt-4o、claude-3-5-sonnet、deepseek-chat、gemini-1.5-pro（用户可通过配置文件添加更多）。

- 用户通过命令行或界面选择主 LLM、开发副 LLM、测试副 LLM 的模型名称，系统校验三者不同。

3.4 方案讨论与 spec 生成

1.  主 LLM 根据需求生成初始技术方案（架构、技术栈、模块划分）。

2.  开发副 LLM 和测试副 LLM 分别对方案进行评审，输出评审意见 JSON（结构见 8.2）。

3.  主 LLM 汇总评审意见，修订方案，生成最终 spec.md 文件。

4.  spec.md 包含：项目目标、用户故事、架构设计、接口定义、数据模型、任务拆分、验收标准。

5.  讨论轮数受第 11 章成本护栏约束（默认 ≤3 轮，循环检测打断，见 11.1 / 11.3）；评审意见 JSON 解析依赖第 15 章输出容错链路。

6.  达到轮数上限或触发循环检测时，主 LLM 收权做最终裁决并直接收敛 spec.md，不再把话题抛回副 LLM。

7.  spec.md 确认环节受收敛约束（见 11.5）。

3.5 代码生成

- 开发副 LLM 读取 spec.md，**按需模块化**生成项目代码文件（启用条件见 12.2）。

- 文件保存到项目目录下的 code/\<module\>/ 文件夹；涉及公共依赖统一放置 \_shared/。

- 必须包含 README.md（说明如何安装依赖、运行项目）和 .env.example（环境变量模板，默认值留空）。

- 生成代码必须遵守总 spec.md 的接口清单（interfaces.json 为唯一合法来源，见第 14 章），不得自创跨模块接口；接口合规为模块完成的前置条件。

- 测试副 LLM 在代码生成后做**静态验证**（语法、import 引用、签名核对，见第 13 章）。

- 代码生成后，系统按当前执行模式给出验证路径（安全模式：提示本地运行路径；自动模式：提交沙箱执行）。

- MVP 阶段模块**逐模块串行生成**（先建立单线程正确的闭环，再谈并行）。

3.6 双模执行验证体系

系统支持任务级粒度的执行模式选择，通过统一的 Executor 抽象层对上层 Agent 透明，兼顾安全性与自动化需求。**实施阶段：安全审阅模式为 MVP；自动验证模式为 Alpha v0.4。**

3.6.1 模式定义与对比

  -----------------------------------------------------------------------------------------------------------
  **维度**     **安全审阅模式（默认）**       **自动验证模式（v0.4）**
  ------------ ------------------------------ ---------------------------------------------------------------
  验证方式     AST 静态检查 + LLM 逻辑审查    静态检查 + 沙箱实际运行代码 / 测试

  反馈来源     用户手动运行后粘贴报错         自动捕获 stdout / stderr / 测试结果

  修复触发     用户显式反馈                   执行失败自动触发修复循环

  Token 预算   标准预算                       标准预算 ×2\~3 倍（UI 明确提示）

  安全护栏     无额外限制                     网络隔离 + 文件系统只读挂载 + 30s 超时熔断 + 高危操作拦截降级

  适用场景     生产代码、敏感项目、初次使用   原型开发、测试项目、已信任代码库
  -----------------------------------------------------------------------------------------------------------

3.6.2 Executor 抽象层

- **统一接口**：run(code, tests, timeout) -\> ExecutionResult（结构定义见 8.4）。

- **安全模式实现**：返回 ExecutionResult(status=\"SKIPPED\", message=\"请手动运行以下命令...\")，附带完整运行指令与预期输出。

- **自动模式实现**：调用本地 Docker 沙箱（选型见 6.4），捕获输出，返回结构化结果。

- **透明性原则**：Dev / Test Agent 无需感知当前模式，仅依赖 ExecutionResult 驱动后续流程。

3.6.3 预算与安全护栏

- 自动模式预算 = 标准预算 ×2\~3（默认 ×2.5，可配置）。依据：自动模式引入测试执行与失败自动触发的修复轮次，修复循环消耗预期约为安全模式的 2 倍；该倍数须在任务开始前于 UI 明示并经用户确认。

- 沙箱护栏属于总则 D.4 的**边界护栏**：网络隔离、文件系统只读挂载、30s 超时熔断、高危操作（如删除、外发请求）拦截并降级告知，不静默自决。

3.6.4 实施要点

- MVP 阶段先实现安全模式，但必须预留 Executor 接口，确保后续接入沙箱时无需重构 Agent 逻辑。

- 自动模式的修复循环严格复用第 11 章护栏，不因「自动」而放开限制（仅调整预算倍数）。

- 模式切换支持任务级粒度，同一项目中不同模块可选择不同模式；模式一经任务开始默认不中途切换，如需切换须用户显式确认。

3.7 增强型测试反馈环

- **自动验证模式（v0.4）**：静态检查通过后自动执行生成的单元测试；测试 Pass / Fail 结果、断言错误信息作为结构化上下文注入修复 Prompt，替代纯文本报错。

- **安全模式兼容（MVP）**：即使不自动执行，也生成可独立运行的测试文件；交付时附带清晰的运行指令与预期输出，降低用户手动验证成本。

- **预期收益**：减少无效人工交互轮次，提升交付代码的逻辑正确率；将「语法正确」升级为「逻辑正确」，缩小 AI 生成代码与可用代码的差距。

3.8 用户反馈与修复

- 安全模式：用户将运行日志、报错信息粘贴回系统；自动模式：ExecutionResult 为 FAILED / TIMEOUT 时自动进入修复流程。

- 系统将反馈（文本或结构化测试结果）交给开发副 LLM，生成修复后的代码文件。

- 修复循环中：修改代码须**同步更新**对应模块规格与变更记录（见 12.7）；测试副 LLM 复验是否满足原验收用例与接口门禁（见第 13、14 章）。

- 修复循环受**最大轮数约束**（默认 ≤5，见 11.4）与**接口校验门禁**（第 14 章）；达到上限仍失败时生成「已知问题与降级方案」交用户决定。

- 同一模块连续修复失败 ≥2 轮时，系统建议启动 Researcher 调研（见 4.5），由用户确认。

- 循环直至用户确认成功、达成上限或用户手动停止。

4\. Researcher 角色与知识注入（实施阶段：Beta v0.5）

4.1 角色定位

可选前置角色，在编程任务前按需激活，不属于固定团队编制。

4.2 核心职责

查阅官方文档、搜索 API 用法、分析竞品实现，输出结构化技术摘要（来源、版本、用法示例、已知坑点）。

4.3 知识注入方式

研究结果作为 System Prompt 或 RAG 上下文注入 Dev / Test Agent，解决 LLM 知识过时问题，提升技术方案可行性与代码质量。

4.4 成本控制策略

- 研究任务设置独立 Token 预算 research_budget（建议默认 20k，可配置），避免无限搜索；该预算独立于任务总预算之外但计入全局消耗日志。

- 研究结果可缓存复用，相同技术栈查询不重复消耗 Token；缓存命中范围按「技术栈 + API 名称 + 版本」三元组判定。

4.5 触发条件（量化）

- 用户显式指定需要调研。

- 主 LLM 判断任务涉及陌生技术栈或新 API（须在评估 JSON 的 reason 中给出判断依据）。

- 同一模块连续修复失败 ≥2 轮时，系统自动建议启动研究（由用户确认后激活）。

4.6 依赖与 MVP 降级

联网调研依赖外部工具调用能力（见 2.3 路线图）。在该能力就绪前，Researcher 可降级为「用户提供资料注入」模式：用户粘贴文档片段，系统完成结构化摘要与注入。

5\. 非功能性需求

- **安全性**：API 密钥通过环境变量配置，不硬编码；生成代码使用环境变量，默认值留空。沙箱模式（v0.4）额外执行 3.6.3 的四项护栏。

- **可维护性**：代码结构清晰，模块分离，便于扩展；文档即代码（总则 P.2）。

- **日志**：所有 LLM 调用、文件操作、错误信息记录到项目目录下的 logs/ 文件夹，并记录 token 累计、预算占用比例、执行模式与 ExecutionResult 摘要。

- **性能**：单次任务耗时控制在 5 分钟内（取决于模型响应时间），不要求实时；单次任务包含硬性轮数上限与成本总预算闸门，保证耗时有界（第 11 章）。

- **成本**：Token 消耗受六层护栏与总预算约束，单次任务消耗可预估、有界（第 11 章）；embedding 比对的开销纳入成本考量，通过缓存与增量控制（见 11.3）。

- **可审计**：每个项目的决策记录、校验结果、消耗明细均可从项目目录回溯。

6\. 技术架构

6.1 技术栈建议

- **后端语言**：Python 3.10+

- **Web 框架**：FastAPI（提供 REST API）

- **前端界面**：Streamlit（快速搭建聊天交互界面，适合 MVP）或简单 CLI（命令行交互）

- **LLM 接入**：使用 litellm 或各模型官方 SDK 统一调用不同模型

- **向量化（可选，按需）**：讨论循环检测采用 embedding 时，经 model_client.py 统一封装在线 embedding 模型（如 text-embedding-3-small）或本地中文模型（如 bge-small-zh）。MVP 阶段可按「A：零依赖文本相似度」或「B：文本 + embedding 双层」二选一，共用同一套循环检测模块接口。

- **数据存储**：本地文件系统（项目目录），使用 JSON 和 Markdown 文件保存数据

- **代码执行**：MVP 为安全审阅模式，不在云端执行代码；建立 Executor 抽象接口，Alpha 版接入本地 Docker 沙箱（选型见 6.4）

6.2 模块划分

> app/
>
> ├── main.py \# 入口，启动 FastAPI 或 Streamlit
>
> ├── config.py \# 读取配置（模型列表、API 密钥、阈值/轮数/预算/模式默认值）
>
> ├── orchestrator.py \# 核心编排逻辑；含「决策走大模型/校验走程序」路由框架
>
> ├── agents/
>
> │ ├── base.py \# Agent 基类
>
> │ ├── main_llm.py \# 主 LLM 角色
>
> │ ├── dev_llm.py \# 开发副 LLM 角色
>
> │ ├── test_llm.py \# 测试副 LLM 角色
>
> │ └── researcher.py \# Researcher 角色（Beta v0.5 预留）
>
> ├── tools/
>
> │ ├── file_manager.py \# 项目目录创建、文件读写（code/\<module\>、modules/、changelog/、\_shared/）
>
> │ └── prompt_templates.py \# 提示词模板（收敛裁决、模块清单、验收用例、修复注入等）
>
> ├── execution/
>
> │ ├── executor.py \# Executor 抽象接口 + ExecutionResult 定义
>
> │ ├── safe_executor.py \# 安全审阅模式实现（返回 SKIPPED + 手动运行指令）
>
> │ └── sandbox_executor.py \# 自动验证模式实现（Alpha v0.4，本地 Docker）
>
> ├── dashboard/
>
> │ └── token_board.py \# 基础 Token 看板（MVP）；高级可视化 Beta 预留
>
> └── utils/
>
> ├── logger.py \# 日志记录
>
> ├── model_client.py \# LLM 调用封装（支持 response_format=json_object 时）
>
> ├── similarity.py \# 循环检测：文本相似度初筛 + 可选 embedding + 向量缓存
>
> ├── parse.py \# JSON 解析四级降级（原生 → 提取块 → 程序容错 → 强制 JSON）
>
> ├── static_check.py \# 静态验证：语法校验（ast.parse）+ import/引用核验
>
> └── interface_check.py \# 接口校验门禁：AST 抽取 + 差异判定 + 接口地图自洽性校验

6.3 项目目录结构（运行时的项目文件）

> projects/{project_id}\_{timestamp}/
>
> ├── spec.md \# 总则：需求、模块清单、依赖/接口地图（主 LLM 维护）
>
> ├── interfaces.json \# 接口地图（程序可读，可信基线，见第 14 章）
>
> ├── modules/ \# 按功能拆分的模块规格，各自独立
>
> │ ├── news_query.md
>
> │ └── sales_report.md
>
> ├── code/ \# 按功能拆分的代码
>
> │ ├── news_query/
>
> │ ├── sales_report/
>
> │ ├── README.md
>
> │ ├── .env.example
>
> │ └── \_shared/ \# 公共层：共享数据源、组件、通用类型
>
> ├── tests/ \# 各模块可独立运行的测试文件与运行指令
>
> ├── changelog/ \# 可按模块回溯修复历史
>
> │ ├── news_query/fix_history.md
>
> │ └── sales_report/fix_history.md
>
> ├── logs/ \# 会话与调用日志 + token 累计/预算占用 + 模式与执行结果
>
> │ └── session_001.log
>
> └── sessions/ \# 会话记录
>
> ├── requirements.md
>
> ├── difficulty_assessment.md
>
> └── discussion_summary.md

6.4 Executor 选型对比（自动验证模式）

  --------------------------------------------------------------------------------------------------------------------------
  **候选方案**       **安全隔离**                         **部署成本**                     **结论**
  ------------------ ------------------------------------ -------------------------------- ---------------------------------
  本地 Docker        容器级隔离，支持网络隔离与只读挂载   需用户本地 Docker 环境           **Alpha v0.4 首选**（默认实现）

  E2B 云沙箱         隔离强、托管运维                     云依赖、额外费用、数据出境风险   Release v1.0 云端沙箱选项的候选

  临时目录直接执行   无隔离                               零依赖                           **禁止用于生产**，仅限内部试验
  --------------------------------------------------------------------------------------------------------------------------

MVP 安全模式不依赖上述任何方案；Alpha 版默认实现本地 Docker，并复用 3.6.3 的四项护栏。

7\. 工作流程（详细步骤）与整体流程图

下图为本 AGENT 的整体工作流程（含任务类型分流、成本护栏、循环检测、接口门禁、修复回路）：

![整体工作流程图](E:\token-burner\media/media/4903028ecf09a75d27fe7e2ba023f95f2900f4ed.png "AGENT 整体流程图"){width="5.166666666666667in" height="9.166666666666666in"}

注：该图为基础编排流程；**双模执行验证分支**发生在步骤 6--8（安全模式 → 用户手动运行反馈；自动模式 → 沙箱执行结果自动反馈，见 3.6）。

步骤 1：用户输入

用户通过界面输入需求，例如：

「帮我开发一个企业内部的糖尿病管理智能体，能够回答员工关于糖尿病的基本问题，并记录每日血糖数据。」

步骤 2：需求识别与评估

主 LLM 调用提示词模板，输出评估 JSON（含任务类型三分类）。系统展示给用户：

> 难度评估：7/10（困难）
>
> 任务类型：编程
>
> 原因：涉及多模块、数据库、权限控制等。

解析若失败，进入第 15 章降级链路（保守默认视作编程 + 用户确认）。

步骤 3：模型与模式选择（仅编程任务）

系统提示选择三个不同的模型分别担任主 LLM（项目经理）、开发副 LLM、测试副 LLM，并选择执行模式（默认安全审阅模式）。用户输入选择后，系统创建项目目录，并先通过**成本总预算闸门**检查（见 11.0；自动模式按倍数调整预算并明示确认）。

步骤 4：方案讨论

1.  主 LLM 生成初始方案（文本），展示在界面。

2.  开发副 LLM 和测试副 LLM 分别评审，输出评分和意见。

3.  每轮结束进行**循环检测**（11.3 节）；达到轮数上限或命中循环则冻结副 LLM 发言、主 LLM 收权裁决。

4.  主 LLM 根据意见生成 spec.md，显示内容，询问用户是否确认或修改（确认环受 11.5 收敛约束）。

步骤 5：用户确认 spec

用户输入「确认」或提出修改意见。若修改，主 LLM 更新 spec.md 后再次请求确认（默认最多 3 次，第 3 次后收敛合并）。

步骤 6：架构设计与代码生成

1.  按复杂度条件判断是否启用**模块化拆分**（12.2 节）：主 LLM 产出模块清单，写入总 spec.md 并生成 interfaces.json。

2.  逐模块：开发副 LLM 依据模块规格生成代码，落入 code/\<module\>/。

3.  测试副 LLM 逐模块静态验证（第 13 章）+ 接口校验门禁（第 14 章）通过后，模块方为完成。

4.  系统按当前模式给出验证路径：安全模式显示代码路径并提示本地运行；自动模式（v0.4）提交沙箱执行。

步骤 7：执行验证与反馈

安全模式：用户运行代码，将输出或报错信息发送给系统。自动模式：沙箱执行并返回 ExecutionResult，FAILED / TIMEOUT 自动进入步骤 8。

步骤 8：修复循环

系统将反馈（文本或结构化测试结果）交给开发副 LLM，生成修改后的代码文件（同步更新模块规格与变更记录，受接口门禁与最大 5 轮上限约束）。再次触发验证，直到用户输入「成功」「停止」，或达成修复上限（此时输出「已知问题与降级方案」）。

8\. 数据模型与文件格式

8.1 难度评估 JSON

> { \"difficulty_score\": 7, \"difficulty_level\": \"困难\", \"task_type\": \"编程\",
>
> \"reason\": \"需求复杂，涉及多模块和数据库\" }

注：task_type 取值为「基础 \| 研究/分析 \| 编程」。

8.2 评审意见 JSON

> { \"scores\": { \"feasibility\": 8, \"security\": 6, \"maintainability\": 7 },
>
> \"strengths\": \[\"技术选型合理\", \"模块划分清晰\"\],
>
> \"weaknesses\": \[\"权限控制描述不够详细\"\],
>
> \"risks\": \[\"数据合规风险\"\] }

8.3 接口地图 interfaces.json（结构化基线）

> {
>
> \"cross_module_symbols\": \[{ \"name\": \"\...\", \"signature\": \"\...\",
>
> \"provider\": \"\...\", \"consumers\": \[\"\...\"\] }\],
>
> \"shared_exports\": \[\"\...\"\],
>
> \"allowed_third_party_imports\": \[\"\...\"\]
>
> }

8.4 ExecutionResult（执行结果）

> {
>
> \"status\": \"SKIPPED \| SUCCESS \| FAILED \| TIMEOUT \| BLOCKED\",
>
> \"exit_code\": 0,
>
> \"stdout\": \"\...\",
>
> \"stderr\": \"\...\",
>
> \"test_results\": \[{ \"name\": \"test_xxx\", \"status\": \"PASS \| FAIL\",
>
> \"assertion\": \"期望值与实际值差异描述\" }\],
>
> \"duration_ms\": 1234,
>
> \"message\": \"安全模式：请手动运行以下命令...（含运行指令与预期输出）\"
>
> }

status 语义：SKIPPED = 安全模式未执行；SUCCESS / FAILED = 沙箱执行结果；TIMEOUT = 触发 30s 熔断；BLOCKED = 高危操作被拦截（见 3.6.3）。

8.5 项目会话记录

- requirements.md：保存用户原始需求和难度评估结果。

- discussion_summary.md：保存方案讨论的摘要和评审意见。

- spec.md：最终技术规格文档。

- interfaces.json：跨模块引用唯一合法来源（第 14 章）。

- changelog/：各模块的修复历史，供后期回溯。

9\. API / 工具集成

- 使用 litellm 统一调用不同模型，API 密钥从环境变量读取。

- MVP 不集成外部工具、不执行代码（安全审阅模式）；外部 API / MCP 工具调用为路线图能力（见 2.3），自 Alpha 起逐步开放，Researcher 联网调研依赖此能力。

- 向量化 embedding（如启用循环检测第 2 层）经 model_client.py 统一封装。

10\. 用户交互细节

10.1 命令行交互（若使用 CLI）

> \$ python main.py
>
> 你好，我是 Jarvis，请描述你的需求：
>
> \> \[用户输入\]
>
> 评估中\...
>
> \[显示评估结果\]
>
> \[若编程任务\] 请选择模型：主 LLM, 开发副 LLM, 测试副 LLM（逗号分隔）：
>
> \> gpt-4o, deepseek-chat, claude-3-5-sonnet
>
> 请选择执行模式：\[1\] 安全审阅模式（默认） \[2\] 自动验证模式（v0.4 开放）
>
> \> 1
>
> 创建项目目录\...（预算闸门检查通过）开始方案讨论\...
>
> \[显示讨论过程\]
>
> 方案已生成，是否确认？(y/n)
>
> \> y
>
> 正在生成代码\...
>
> 代码已保存至 projects/xxx/code/
>
> 请本地运行，然后反馈结果。输入 \'exit\' 结束。
>
> \> \[用户反馈\]\...

10.2 Web 界面（若使用 Streamlit）

- 文本框输入需求，按钮提交；显示评估结果。

- 若编程任务，显示模型选择下拉框与执行模式选择（含模式说明与预算提示）。

- 讨论过程以日志形式展示；提供确认按钮和输入框用于修改意见。

- 显示代码路径与运行指令，提供反馈输入框。

- 侧边栏显示基础 Token 看板（当前消耗 / 预算剩余）与当前模式状态标识（见第 16 章）。

11\. 成本控制（Token 六层护栏）

为控制多智能体协作的 API 消耗，采用统一收敛的六层结构：**总预算闸门（第 0 层）→ 讨论轮数上限（第 1 层）→ 单轮对话 Token 上限（第 2 层）→ 循环检测打断（第 3 层）→ 修复循环上限（第 4 层）→ spec 确认收敛（第 5 层）**。各层独立触发，以总预算为最终底线；在安全 / 自动两种模式下均生效，自动模式仅调整预算倍数，不取消限制。

11.0 单任务成本总预算（第 0 层，总闸）

各护栏均是独立参数，乘法组合下最坏消耗仍可能失控。故设**单任务 token 总预算** max_task_tokens 作为全局兜底，先于各层护栏生效：

- **默认值**：max_task_tokens 默认 200k（可配置），覆盖该任务所有 LLM 调用（含讨论、spec、代码生成、修复、embedding 比对）的累计 output + input。**自动验证模式按 ×2\~3 倍调整（默认 ×2.5）**，依据见 3.6.3。

- **触发行为**：orchestrator 在每步调用前累加估算，接近预算（如 ≥90%）时进入**省 token 模式**：压缩讨论轮数、跳过非必要 embedding 比对、生成提示统一收敛；一旦**超预算**，立即中止该任务并落盘「已完成部分 + 未完成清单 + 已耗 token」，交用户决定续跑或止损。

- **与各层护栏的关系**：各护栏不再单独决定「何时截止」，而是由 max_task_tokens 统一收敛；仍各自独立触发，但以总预算为最终底线。

- **可观测性**：logs/ 记录每步 token 累计与预算占用比例，便于事后校准默认值。

11.1 讨论轮数上限（第 1 层，硬性）

- **「1 轮」的统一定义**：主 LLM 提出初始方案 → 开发副 LLM 评审 → 测试副 LLM 评审 → 主 LLM 汇总修订为一个评审周期。完整跑一遍记为目标轮数 +1。

- 默认最大轮数：**3 轮**（max_discussion_rounds，可在配置中调整）。

- 达标行为：第 N 轮汇总时，主 LLM 必须直接产出收敛的最终 spec，不允许再提出新的开放问题。

- 目的：让单次讨论的 Token 消耗量预先可预估，不因「讨论不尽兴」而无限膨胀。

11.2 单轮对话 Token 上限（第 2 层，硬性）

- 单次 LLM 调用的输出上限 max_response_tokens 默认 **8k**（建议值，可配置），通过 litellm 的 max_tokens 参数与提示词篇幅约束实现。

- 依据：限制单次响应失控（如代码生成一次性倾泻超长内容）挤占整体预算；与总预算闸门互补------总预算管累计，本层管单点峰值。

- 触发行为：输出被截断时按「未完成」处理，由 orchestrator 决定分块续写或重试，不静默丢弃。

11.3 讨论循环检测（第 3 层，软性，提前止损）

**设计原则**：由程序负责判定，LLM 不自我判断（LLM 无法可靠感知自身重复）。

- **检测粒度**：按「关键论点」切分比对，而非整段发言。仅对「这一轮新出现的论点」做检测。

- **双层检测（先廉后贵）**：1）文本相似度首道拦截（零依赖）：对论点分词后计算 Jaccard 重合度，重合度 \> 阈值 0.9 直接判定字面重复；2）embedding 语义拦截（第二道）：首道未命中时，将该论点向量化，与历史论点库计算余弦相似度，相似度 \> similarity_threshold（MVP 默认 0.85，可配置）判定语义重复。

- embedding 统一经 model_client.py 封装调用；若预期中文需求为主，可选本地中文模型以提升中文效果。

- **缓存与增量**：每轮论点向量落地缓存，新一轮仅对新论点做向量化比对，避免重复计算全部历史，压缩 embedding 自身成本。

- **判定与打断**：同一论点重复计数 \< N 次（MVP 默认 3）：仅记录计数，正常进入下一轮；同一论点重复 ≥ N 次，或目标轮数耗尽：**冻结全部副 LLM 发言权**，由主 LLM 基于已积累论点直接做最终裁决并结束讨论。

- **参数集中管理**：similarity_threshold、Jaccard 阈值、重复次数上限 N、最大轮数全部放入配置项，便于实测后调优。

11.4 修复循环上限（第 4 层，硬性）

- 代码「生成 → 验证反馈 → 修复」循环默认最多 **5 轮**（max_fix_rounds，配置可调）。

- 达到上限仍失败时：不再无限修复，生成一份「已知问题与降级方案」说明，交予用户决定（手动放开或止损）。

11.5 spec 确认循环收敛（第 5 层，硬性）

- 用户在 spec 确认环节提出的修改意见，默认最多 **3 次**（max_spec_confirm_rounds）。

- 第 3 次后由主 LLM 主动收敛合并意见，输出最终 spec，不再反复征询。

11.6 护栏总表

  -------------------------------------------------------------------------------------------------------------------------------------------------
  **层**   **名称**              **参数**                             **默认值**              **性质**   **达标行为**
  -------- --------------------- ------------------------------------ ----------------------- ---------- ------------------------------------------
  0        单任务成本总预算      max_task_tokens                      200k（自动模式 ×2.5）   总闸       ≥90% 省 token 模式；超预算中止落盘交用户

  1        讨论轮数上限          max_discussion_rounds                3 轮                    硬性       主 LLM 直接产出收敛 spec

  2        单轮对话 Token 上限   max_response_tokens                  8k                      硬性       截断按未完成处理，分块续写或重试

  3        讨论循环检测          similarity_threshold / Jaccard / N   0.85 / 0.9 / 3 次       软性       冻结副 LLM 发言，主 LLM 收权裁决

  4        修复循环上限          max_fix_rounds                       5 轮                    硬性       输出「已知问题与降级方案」

  5        spec 确认收敛         max_spec_confirm_rounds              3 次                    硬性       主 LLM 收敛合并，输出最终 spec
  -------------------------------------------------------------------------------------------------------------------------------------------------

12\. 模块化项目治理

为达成「代码结构清晰、可单独修改、单独验收、单独追踪；后期好迭代；改动局部功能不影响全局」，将每个任务拆为若干**功能模块**，每个模块自成一个「独立目录 + 独立规格 .md + 独立代码 + 独立修复记录」的单元。模块化拆分**按复杂度条件启用**，不求一律拆。

12.1 核心目标

- **干净**：代码按功能分目录落盘，不把全部代码堆在一个顶层目录。

- **可查**：每个功能有自己的规格 .md 和历次修复记录，后期能按「功能 → 规格 → 代码 → 变更记录」回溯问题。

- **可迭代**：修改个别模块不牵动全局。

- **可验收**：模块级独立验收，单独运行不影响其他模块。

12.2 启用条件

仅当任务复杂度达到阈值时启用模块化拆分（默认：主 LLM 难度分 ≥ 5，或预估产出文件数 ≥ 6，可配置）。简单任务仍走单份 spec 直出，避免不必要地增加编排与 Token 成本。（与 3.2 节 simple_threshold 联动，消除阈值矛盾，见 3.2「阈值统一」。）

12.3 项目目录结构

见第 6.3 节目录树（含 modules/、code/\<module\>/、\_shared/、changelog/、tests/）。

12.4 职责归属

- **主 LLM**：任务拆分，产出模块清单 JSON（模块名、职责、依赖、验收标准），写入总 spec.md 的模块清单与依赖 / 接口地图；维护全局 \_shared/ 接口，确保跨模块一致。

- **开发副 LLM**：逐个模块读取对应 modules/\<module\>.md 生成代码，落入 code/\<module\>/；代码生成与修复必须遵守总 spec.md 的接口清单，**不得自创跨模块接口**（其合规性由接口校验门禁强制执行，见第 14 章）。

- **测试副 LLM**：按模块独立验证（函数签名、依赖引用、验收入口），不进行整包一次性验收。其贯穿全流程的角色见第 13 章。

12.5 接口隔离（这是「改 A 不动 B」的真正保障）

- 模块之间只通过总 spec.md 中声明的接口通信，不直接触碰对方内部实现。

- 公共依赖**集中归口** \_shared/，主 LLM 在总 spec 声明其接口；模块不各自持有副本，避免重复与不一致。

- \_shared/ 被多模块共用，**其变更牵动全局**：修改它必须最谨慎，并触发对依赖模块的回归检查（见 12.7）。

- 抑制剂：对强耦合单体任务不强拆，模块化反而增加接口维护成本，此时按整体处理更省心。

12.6 独立 md 的粒度约定

- 「独立文件」粒度以**能被单独定位、单独修改、单独验收**为准。功能模块（如 news_query、sales_report）够格。

- 过碎不拆：临时小工具、少量配置、一行常量，没必要单独建 md，避免文件爆炸反而更乱。

- 过粗不拆：模块应能独立交付或具备明确完整边界。

12.7 变更联动与文档同步

- 修复循环中修改代码时，必须**同步更新**对应 modules/\<module\>.md 与 changelog/ 记录，防止文档与代码脱节、误导后续调用（文档即代码，总则 P.2）。

- 模块接口一旦变更，主 LLM 检查是否影响依赖它的其他模块，必要时联动提醒并在总 spec.md 更新接口地图。

13\. 测试角色与质量把关

测试副 LLM 是**贯穿全流程的质量把关者**。尊重 MVP 边界（安全模式不执行代码），其「测试」以**生成测试用例 + 静态 / 逻辑验证**为主，实际运行在安全模式下交由用户本地完成、在自动模式（v0.4）下由沙箱完成。

13.1 职责贯穿流程

- **方案评审**：保留对方案的可行性 / 安全性 / 可维护性打分。

- **spec / 模块清单生成后**：依据总 spec.md 与各模块 modules/\<module\>.md，为每个模块生成**验收测试用例**（输入 / 预期输出 / 边界）与静态检查清单，并落盘为可独立运行的测试文件（tests/）。

- **代码生成后**：逐模块执行**静态验证**------语法合法性、import 与依赖引用是否存在、函数签名是否与总 spec 接口地图一致。

- **验证与修复中**：复验修复是否满足原验收用例、是否引入新的不一致；通过后方标记该模块完成。

13.2 验证手段（双模）

- **安全模式（MVP）**：结构核验（比对代码引用的函数、模块、import 与 spec 接口地图）；python ast.parse() 语法校验、简单 import / 引用解析（零执行，非沙箱）；辅以测试副 LLM 的逻辑审查。

- **自动模式（v0.4）**：静态检查通过后，沙箱真实执行 tests/ 中的单元测试，返回结构化结果（见 8.4）。

- 产出「验收清单 / 验证报告」写入 changelog/ 或 sessions/，作为后期回溯依据。

13.3 增强反馈注入

自动模式下，测试 Pass / Fail 结果与断言错误信息作为**结构化上下文注入修复 Prompt**，替代纯文本报错，使开发副 LLM 获得精确的失败定位（见 3.7）。

13.4 与模块化治理衔接

测试**按模块独立**进行（呼应 12.4）；模块修复只重验该模块及其依赖者，符合 12.5 / 12.7 的 \_shared/ 回归要求。

13.5 边界与例外

- MVP 不自动运行测试用例，仅生成并静态验证；真实运行结果在安全模式下由用户本地反馈（沿用原交互）。

- 验证发现的问题反馈开发副 LLM 修复，进入修复循环（受成本护栏 11.4 约束）。

14\. 接口与依赖校验门禁

12.4 节规定「开发副 LLM 不得自创跨模块接口」，但这是纪律，不是机制。本门禁用**确定性 AST 校验**把这条纪律变成硬门禁：以接口地图为唯一合法来源，对模块代码做引用抽取与差异判定，违规即阻断进入修复循环。全程零执行、零 LLM 调用，是整套方案中唯一不增加 token 的纪律执行器，契合成本目标。这属于合规校验而非抢走接口设计决策（见 D.3）。

14.1 接口地图（可信基线）

- 总 spec 里的「接口清单」不能停留在自然语言，必须落地为程序可读的**结构化文件** interfaces.json（由主 LLM 生成、测试副 LLM 校核，纳入总 spec）。

- 至少包含三类：1）**授权跨模块符号**：符号名 + 签名（参数个数 / 名称 / 返回）+ 声明提供方模块；2）**\_shared/ 导出面**：允许各模块引用的公共符号白名单；3）**外部依赖白名单**：允许 import 的第三方包与标准库。

14.2 引用抽取与差异判定（AST，零执行）

对每个模块代码做 AST 抽取：import、跨模块调用、对 \_shared/ 的引用。计算「实际引用集 − 合法集 = 违规集」，判定四类结果：

  ----------------------------------------------------------------------------
  **结果**                                        **类型**       **严重度**
  ----------------------------------------------- -------------- -------------
  引用接口地图之外的项目内符号（自创接口）        违规           阻断

  import 白名单外的新第三方包                     违规           阻断

  引用已登记符号但签名不匹配（参数 / 返回不符）   违规           警告

  引用与接口地图一致                              通过           ---
  ----------------------------------------------------------------------------

输出违规清单。

14.3 接口地图自洽性校验（防基线自身错误）

- 统计每个符号的「声明方-消费方」对称性：发现**悬空接口**（声明了无消费）与**幽灵引用**（有引用未声明）。

- 主 LLM 生成的接口地图须通过此自洽校验，否则退回修正，避免拿错误基线当裁判；测试副 LLM 参与抽查。

14.4 门禁接入流程

- 模块静态校验**无阻断项** = 该模块完成的前置条件，接入第 13 章的复验门禁。

- 违规直接反馈开发副 LLM 修复，计入修复循环并受 11.4 上限约束。

- \_shared/ 变更时，触发对其全部依赖模块的**整包回归**（呼应 12.7）。

14.5 MVP 幅度

- 首期仅覆盖「跨模块符号 + \_shared/ 导出面 + 第三方依赖白名单」三类引用；依赖者整包回归仅在 \_shared/ 变更时触发。

- 模块**逐模块串行生成**，接口地图随生成顺序逐步落定；待单线程闭环正确后再谈并行。

15\. 输出容错（JSON 解析降级策略）

流程中多处要求 LLM 输出 JSON（难度评估、评审意见、模块清单等），LLM 输出常不稳定（多余文本、缺右括号、引号转义错误、嵌套错误）。本章提供**从宽松解析到硬回退**的降级链路，确保单次解析失败不会卡死整条流程，且失败路径可被观测记录。

15.1 解析策略（由宽松到严格逐步尝试）

按以下顺序依次尝试，命中即采用，全部失败才进入 15.2 / 15.3：

1.  **原生解析**：直接 json.loads(\...)，成功即用。

2.  **提取 JSON 块**：用正则提取文本中的 {\...} / \[\...\] 最外层块后解析。

3.  **程序容错修复（确定性，零 token）**：对常见损坏做轻量修补后重试------补缺失的右括号、将裸单引号替换为双引号、去除末尾逗号、修正未转义引号。此类为可枚举的确定性操作，依总则必须由程序完成。

4.  **强制 JSON 响应**：当模型支持（如 OpenAI / 兼容接口）时，请求携带 response_format={\"type\": \"json_object\"} 从源头约束；不支持的模型在 prompt 中加入 few-shot 示例。

15.2 LLM 辅助修复（可选兜底，默认关闭）

仅当 15.1 四级全部失败、且进入重试前，可配置调用一次 LLM 修复（将损坏文本交给模型重建为合法 JSON）。**默认关闭**：格式修复属确定性操作，程序修复优先；开启时该调用计入 token 统计与日志，避免以「容错」之名行「用 Token 做简单校验」之实（违背总则）。

15.3 重试与硬回退

- **单次调用最大重试次数**：默认 **3 次**（可配置）；重试时替换为更严格的 prompt（追加「仅输出合法 JSON，无其他文字」）。

- **重试仍失败 → 硬回退**：按下游用途降级，保证流程能继续：

- **难度评估**：放弃 JSON，降级为纯文本结果，任务类型按**保守默认**（一律视为编程任务）+ 提示用户手动确认，避免错误放行。

- **评审意见 / 模块清单**：降级为不解析，主 LLM 依据副 LLM 原始文本自行归纳，不再强求结构化打分。

- **无法降级的关键点**（如 orchestrator 分支所依赖的字段缺失）：中断该分支，给出明确的失败信息供用户与日志排查，而不是静默出错。

15.4 解析失败的可观测性

- 每次解析失败均记录：触发位置、失败阶段、采用的降级策略、最终结果，写入 logs/。

- 便于后期统计「哪些环节最易发生解析失败」，针对性优化 prompt。

15.5 参数与依赖

- 配置项：最大重试次数、是否启用强制 JSON 响应、程序容错修复开关、LLM 辅助修复开关（默认关）。

- 解析与容错逻辑收敛进独立模块 utils/parse.py，对外暴露 parse_json(text) -\> (value \| None, detail)；供难度评估、评审、模块清单等所有 JSON 产出点复用。

16\. 可视化调试与成本看板（分阶段）

16.1 基础 Token 看板（MVP）

- 展示当前任务 Token 消耗、预算剩余与占用比例。

- 以文本 / 简单进度条形式集成于 CLI 输出或 Web 侧边栏，不引入额外重依赖。

16.2 实时监控面板升级（Beta v0.5）

- 全局消耗、各 Agent 占比；按时间轴查看消耗趋势，识别异常峰值。

16.3 Agent 对话流图（Beta v0.5）

- 可视化消息流转路径、决策节点、修复循环次数。

- 高亮显示循环检测触发点与预算告警事件，便于定位瓶颈。

16.4 模式状态指示（Alpha v0.4，随沙箱交付）

- 清晰标识当前处于安全 / 自动模式。

- 沙箱运行状态实时反馈（启动中 / 运行中 / 超时 / 失败）。

16.5 技术选型与解耦

- 推荐使用 Streamlit / Gradio 快速搭建，与现有 CLI 并行提供。

- 可视化层与核心逻辑解耦（仅读取 logs/ 与运行时状态），不影响系统稳定性与性能。

17\. 开发任务清单

第一阶段：基础框架

- 搭建项目结构，配置依赖。

- 实现 config.py，支持从 .env 读取模型 API 密钥，并集中管理各类阈值 / 轮数 / 预算 / 模式默认值（max_task_tokens、max_response_tokens、simple_threshold、相似度阈值、重试次数、预算倍数等）。

- 实现 model_client.py，封装 LLM 调用（使用 litellm），并在模型支持时携带 response_format=json_object。

- 实现 file_manager.py，创建项目目录、读写文件（支持 code/\<module\>/、modules/、tests/、changelog/、\_shared/）。

- 实现 utils/parse.py：四级解析降级（原生 → 提取块 → 程序容错修复 → 强制 JSON）+ 失败记录；LLM 辅助修复默认关闭。

- 实现 execution/executor.py 与 safe_executor.py：Executor 抽象接口 + ExecutionResult + 安全模式实现（返回 SKIPPED + 手动运行指令）。

第二阶段：核心流程

- 实现主 LLM 需求评估提示词模板，输出 task_type 三值（基础 / 研究·分析 / 编程）并附判断依据。

- 实现难度评估逻辑（主 LLM 返回 JSON）+ 三分类路由 + 「研究 / 分析」独立处理分支。

- 实现简单任务分支：主 LLM 生成结果并输出；边界处由程序触发主 LLM 复核而非自判。

- 实现编程任务分支：模型选择、执行模式选择、创建项目目录、成本总预算闸门检查。

- 实现方案讨论：初始方案 → 双方评审 → 主 LLM 汇总；讨论轮数上限 + 单轮输出上限 + 循环检测打断 + 收敛裁决。

- 生成 spec.md，接入用户确认收敛（≤3 次）。

第三阶段：代码生成、验证与反馈循环

- 实现主 LLM「模块清单 JSON」拆分提示词模板 + 按复杂度条件自动决定是否启用模块化拆分。

- 定义 interfaces.json 数据结构 + 主 LLM 生成本节的提示词模板。

- 实现开发副 LLM 生成代码的提示词模板（输入 spec / 模块规格，输出文件列表及内容），逐模块串行生成。

- 实现测试副 LLM「验收测试用例生成」提示词模板（落盘 tests/），及生成后静态验证（utils/static_check.py：ast.parse + import / 引用核验）。

- 实现 utils/interface_check.py：AST 抽取 + 三类差异判定 + 接口地图自洽性校验（悬空接口 / 幽灵引用）。

- 将接口校验接入模块完成前置门禁，并实现 \_shared/ 变更触发整包回归。

- 实现用户反馈处理（安全模式）：接收反馈，调用开发副 LLM 修复（受 11.4 上限），修复循环中强制同步模块 md 与 changelog，接入「完成前需通过测试副 LLM 复验」门禁。

- 实现 utils/similarity.py：文本相似度初筛 + 可选 embedding 语义比对 + 向量缓存。

- 落地验证报告写入 changelog/ 或 sessions/。

- 在 orchestrator 实现「决策走大模型 / 校验走程序」路由框架（含边界复核触发接口）。

- 记录所有交互日志 + 每步 token 累计与预算占用比例 + 模式与 ExecutionResult 摘要。

- 实现基础 Token 看板（dashboard/token_board.py）。

第四阶段：用户界面

- 实现 CLI 交互（推荐先做 CLI，简单快速），含模式选择与预算提示。

- 或实现 Streamlit 界面（可选），含基础看板与模式状态标识。

- 测试完整流程，修复 bug。

后续阶段（按第 18 章路线图）

- Alpha v0.4：sandbox_executor.py（本地 Docker）+ 自动验证模式 + 测试真实执行与结构化反馈注入 + 模式状态指示。

- Beta v0.5：Researcher Agent + 高级可视化面板 + 模式智能推荐。

- Release v1.0：云端沙箱选项 + 插件化角色扩展 + 成本优化。

18\. 实施优先级路线图

  ------------------------------------------------------------------------------------------------------------------------------------------------
  **阶段**   **版本**           **核心目标**                           **关键交付物**
  ---------- ------------------ -------------------------------------- ---------------------------------------------------------------------------
  MVP        v0.3.1（本文档）   验证核心哲学与双模架构，跑通核心闭环   核心协作闭环 + 安全审阅模式完整实现 + Executor 抽象接口 + 基础 Token 看板

  Alpha      v0.4               闭环自动化能力                         本地 Docker 沙箱集成 + 自动验证模式 + 测试真实执行反馈环 + 模式状态指示

  Beta       v0.5               智能化与体验升级                       Researcher Agent + 高级可视化面板 + 模式智能推荐

  Release    v1.0               产品化与生态                           云端沙箱选项（E2B）+ 插件化角色扩展 + 外部工具调用全量开放 + 成本优化
  ------------------------------------------------------------------------------------------------------------------------------------------------

19\. 风险与注意事项

- **LLM 输出稳定性**：使用结构化提示词并要求 JSON 输出，但需处理解析失败（重试或回退）------通过第 15 章降级链路解决。

- **API 成本**：MVP 可能消耗较多 token，通过第 11 章六层护栏 + 总预算闸门控制，单次任务消耗可预估、有界。

- **embedding 自身成本**：循环检测的 embedding 调用是新增费用项。通过「论点级切分 + 向量缓存 + 增量比对」控制调用量；若实测成本仍不可接受，可降级回纯文本相似度模式。

- **模型依赖**：不同模型 API 格式略有差异，统一使用 litellm 可简化。

- **上下文长度**：方案讨论可能超出上下文，需限制输出长度（11.2 单轮上限配合），必要时摘要。

- **模块化边界**：对强耦合任务不强拆；\_shared/ 变更牵动全局，需最谨慎并触发依赖模块回归。

- **代码质量门禁**：生成代码交付前做静态预检（语法、import、接口），减少无意义修复轮数；安全模式不引入云端执行。

- **用户教育**：在提示中清晰说明操作步骤，降低使用门槛。

- **沙箱安全（Alpha）**：容器逃逸、资源滥用、数据外发风险------以网络隔离、只读挂载、超时熔断、高危拦截四项护栏应对（3.6.3）；用户数据边界需在交付文档中明示。

- **自动模式成本放大**：预算 ×2\~3 必须在任务开始前明示并经用户确认，避免成本预期落差。

- **模式误选**：敏感项目误用自动模式------默认安全模式 + 切换时显式警示（渐进式信任的产品化体现）。

- **Researcher 知识质量（Beta）**：调研结果可能有误------要求标注来源与版本，注入前可经用户确认。

20\. 未来扩展方向

- 本地知识库上传与分块检索

- 副 LLM 能力互联网调研与智能匹配

- 可视化多智能体讨论界面

- 投票机制与决策记录（可作为现有「循环检测 + 主 LLM 收权」机制的后续增强，见 11.3）

- 工具 / MCP 调用与云端沙箱执行

- 动态调整团队成员

- 多项目并行管理（建立在「逐模块串行正确」基础上的并行化）

- 多人协作与权限管理

附录 A：术语表

- **Executor**：执行器抽象层，封装代码运行环境，对上层 Agent 屏蔽安全 / 自动模式差异。

- **ExecutionResult**：Executor 的统一返回结构（见 8.4），驱动后续验证与修复流程。

- **安全审阅模式**：默认执行模式，静态检查 + 用户手动运行反馈，不执行代码。

- **自动验证模式**：沙箱实际执行代码与测试，自动捕获结果并触发修复（Alpha v0.4）。

- **Interfaces.json**：模块间接口契约文件，系统唯一真理源。

- **论点级循环检测**：基于语义相似度而非字面匹配的对话循环识别机制。

- **渐进式信任**：系统设计原则，允许用户根据信任度动态选择自动化级别。

- **文档即代码**：接口契约与模块文档是系统唯一真理源，代码变更必须同步更新。

- **简单编程节流**：难度分 ≤3 的编程任务由主 LLM 直接产出、跳过完整团队以节省 token。

附录 B：版本合并说明

B.1 版本沿革

- v0.2（2026-08-25，整理版）：实施级 MVP 开发规格，含完整实施细节。

- v0.3：演进提案，新增双模执行、增强测试反馈环、Researcher、可视化看板与路线图，但未承接 v0.2 约 80% 的实施细节。

- v0.3.1（本文档，2026-08-26）：合并版。保留 v0.2 全部实施细节，融入 v0.3 新机制，落实已确认决策，修复对比报告指出的问题。自包含，替代双版并存。

B.2 合并时的关键修订

  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **事项**           **v0.2**                        **v0.3**                                 **本合并版**
  ------------------ ------------------------------- ---------------------------------------- -------------------------------------------------------------------------------------------
  护栏体系           五层（含讨论轮数、spec 收敛）   四层（增删未声明）                       统一六层（第 11 章）：恢复讨论轮数上限与 spec 确认收敛，新增单轮对话 Token 上限并补充依据

  JSON 容错第 3 级   程序容错修复（零 token）        LLM 修复                                 恢复程序容错修复；LLM 修复降为可选兜底且默认关闭（15.2）

  沙箱               明确排除                        引入双模规划                             解除排除：MVP 安全模式 + Executor 接口预留，Alpha 接入本地 Docker（3.6 / 6.4）

  外部工具调用       明确排除                        未明示                                   改为路线图条目，Alpha 起逐步开放；Researcher 依赖此能力（2.3）

  可视化界面         明确排除                        新增看板章节                             保留并分阶段：MVP 基础看板，Beta 高级可视化（第 16 章）

  MVP 定义           核心协作闭环                    安全模式 + 接口 + 看板（未含闭环说明）   统一定义：核心闭环 + 安全模式 + Executor 接口 + 基础看板（2.4）

  决策哲学           D.1--D.4 完整总则               压缩为一句                               完整恢复 D 节，并将约束关系扩展至双模与沙箱护栏（总则）

  Researcher 触发    ---                             「失败率过高」未量化                     量化为「同一模块连续修复失败 ≥2 轮」，独立预算默认 20k（4.4 / 4.5）
  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

详细差异分析见《v0.2 → v0.3 版本对比分析报告》（2026-08-26）。
