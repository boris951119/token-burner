# 模型 API 密钥（按需填写，默认留空；密钥仅从环境变量读取，禁止写入代码或 config.json）
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
GEMINI_API_KEY=

# GitHub 推送令牌（scripts/git_push_api.py 读取；最小权限：仅勾选 repo scope）
# 注意：PAT 只写在这里（.env 已 gitignore 不入库），不要粘贴到任何对话/文档中
GITHUB_TOKEN=
