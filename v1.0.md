# token-burner v1.0 Release · 任务清单与优先级

> 版本定位：**从 Beta 到 Release 的质量收敛**——修复 v0.5 真实验收暴露的三大质量缺口，
> 把模块通过率从 ~~0% 收敛到可发布水位，以真实任务基准作为 Release 的硬证据。
> 规划周期：约 8~~10 次会话（按批次排队，见 v1.0-workplan.md）
> 前置基线：v0.5.0-beta 全量交付 + 验收闭环（833 项测试 / 826 passed / 0 failed）

***

## 一、版本目标

### 1.1 核心目标

- **\_shared 覆盖破坏修复**（v0.5 验收真实缺口，用户确认必修）：公共层写入从
  「整文件覆盖」升级为「符号级合并守卫」——LLM 重写丢旧函数在落盘前被拦截与合并；
  配套全局链接门禁，跨模块 import 断裂在门禁处确定性拦截（含 FROZEN 模块）。
  哲学不变：**决策归 LLM（写什么），校验归程序（不许断链）**。

- **接口门禁收敛率提升**（本版最大头）：v0.5 实测契约函数式 API vs LLM 类式实现
  5 轮不收敛（8/8 FROZEN）。四路并进：拆分阶段契约风格约束、门禁失败报告
  从「清单」升级为「修改指导」、契约风格可配置（含 auto 自适应）、修复循环
  上下文增强。

- **平台可移植性**：提示词注入目标平台约束 + 危险扫描增加平台不可用模块清单
  （v0.5 实测生成代码含 Unix-only `fcntl`）。

- **JS/TS 静态危险扫描**（v0.4 已知边界遗留）：Node 代码的首道防线从
  「仅容器隔离」补全为「静态扫描 + 容器隔离」双层。

- **真实任务基准**：10 个标准需求 × auto 模式全跑，模块通过率 ≥60% 作为
  Release 硬指标；成本与成功率数据归档，作为后续版本与对外沟通的基线。

### 1.2 关键指标（KPI）

| 指标                | 当前 v0.5 实测                                  | v1.0 目标        | 衡量方式                    |
| ----------------- | ------------------------------------------- | -------------- | ----------------------- |
| 模块通过率（终态非 FROZEN） | 0%（cmd 项目 8/8 FROZEN；todo 7/7、contacts 5/7） | **≥60%**       | 标准需求集 10 个 × auto 全跑    |
| 跨模块 import 断裂     | 实测 1 次（validate\_isbn 丢失未被拦截）               | **0**（门禁确定性拦截） | 全局链接门禁 + 回归测试           |
| 门禁修复收敛轮数          | 5 轮不收敛（风格冲突型失败）                             | 风格型失败 ≤2 轮收敛   | 基准任务修复轮统计               |
| 平台不可用 import      | 实测 1 次（fcntl on Windows）                    | 0（扫描拦截）        | scan\_dangerous 平台清单    |
| 测试基线              | 833 项（826 passed）                           | ≥880 项且无回退     | `pytest --collect-only` |
| 预算命中率             | 未统计                                         | 超支率 <10%       | 基准任务 cost\_report 统计    |

### 1.3 明确不做（顺延 v1.1+）

- 通用 MCP / 外部 API 工具调用通道（用户决策：不进 v1.0）

- 本地知识库上传与分块检索（RAG）、副 LLM 能力自动匹配（规格 §2.3 排 v1.0 之后）

- 动态团队编制、多人协作与权限管理、投票机制

- 代码签名证书采购（商业化动作，随发布节奏单独决策，不在工程版本内）

***

## 二、任务总览

### 2.1 按优先级统计

| 优先级    | 数量       | 说明                                                         |
| ------ | -------- | ---------------------------------------------------------- |
| P0 必须做 | 6 项      | \_shared 合并守卫、全局链接门禁、契约风格约束、门禁报告增强、平台上移门禁链、真实任务基准          |
| P1 应该做 | 7 项      | 契约风格可配置、修复上下文增强、平台约束注入、平台扫描清单、resume 预算恢复、LLM 逻辑审查、JS 静态扫描 |
| P2 可以做 | 2 项      | 发布流水线复验、文档口径同步                                             |
| **合计** | **15 项** | M14\~M17 四个模块（2026-09-03 组件评审追加 M14-5/6/7）                 |

### 2.2 模块划分

| 模块             | 任务数 | 说明                                  |
| -------------- | --- | ----------------------------------- |
| M14 质量修复       | 7   | v0.5 验收缺口 + 2026-09-03 组件评审追加项的机制修复 |
| M15 门禁收敛率提升    | 4   | 本版核心：让 LLM 能按契约收敛                   |
| M16 JS/TS 静态扫描 | 1   | v0.4 遗留边界补全                         |
| M17 Release 收口 | 3   | 基准验证 + 发布物                          |

***

## 三、详细任务清单

### M14 · 质量修复三件套（v0.5 验收发现）

> 根因（v0.5 实测取证）：`DevLoop._split_shared` → `write_shared_file` 为整文件覆盖
> 写入，后续模块的 LLM 输出只含自己上下文的函数，直接冲掉先前模块写入的符号
> （validate\_isbn 丢失）；且 FROZEN 模块不再重验，断裂无人发现。

| #     | 任务                                                                                                                                                                                                                                                       | 优先级 | 依赖       | 验收标准                                                    |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | -------- | ------------------------------------------------------- |
| M14-1 | **\_shared 符号级合并守卫**：`write_shared_file` 落盘前 AST 对比新旧版本——新版本缺失的既有顶层符号（函数/类/常量）自动保留合并；同名符号内容变更则采用新版本并记录 diff 日志；语法解析失败回退整文件覆盖（保持旧行为，不阻塞）                                                                                                                  | P0  | 无        | 单测：新版本丢函数 → 旧函数保留；同名变更 → 采用新版；SyntaxError → 覆盖回退        |
| M14-2 | **全局链接门禁**：门禁阶段（静态验证后）对**全部已落盘模块**（含 FROZEN）执行 import 符号解析——模块间与 `_shared` 的被引符号必须存在于来源文件；缺失 → 阻断当前模块并输出精确缺失清单（文件+符号+来源）                                                                                                                                 | P0  | M14-1    | 复现 v0.5 断裂场景（删 validate\_isbn）→ 门禁确定性拦截；正常项目零误报         |
| M14-3 | **平台约束注入**：新增配置 `target_platform`（缺省 `windows`，可选 `linux`/`macos`/`any`）；write\_code / fix\_code / write\_tests 提示词注入平台约束段（禁用目标平台不可用标准库）                                                                                                                 | P1  | 无        | fcntl 类需求重跑 → 生成的锁实现改用平台可用方案；非 windows 目标不注入该约束         |
| M14-4 | **危险扫描平台黑名单**：`scan_dangerous` 增加平台不可用模块清单（windows: fcntl/termios/pwd/grp/resource…；linux: msvcrt/winreg…），命中 → BLOCKED 并给出平台提示                                                                                                                          | P1  | M14-3 同步 | 单测：含 `import fcntl` 的代码在 windows 目标下 BLOCKED；`any` 目标放行 |
| M14-5 | **平台黑名单上移门禁链**（2026-09-03 组件评审追加）：平台/危险预扫描目前仅在 Local/DockerExecutor（auto 模式）执行，**safe 是默认模式却不覆盖**——fcntl 代码可静默通过全部门禁直到用户手动运行才炸。将扫描上移至 dev\_loop 门禁链（静态→链接→接口之后），双模式统一覆盖；命中 → 进修复循环（LLM 换平台可用方案）而非直接冻结                                                    | P0  | M14-4    | safe 模式下单测：fcntl 代码被门禁拦截并进修复循环（此前静默放行）                  |
| M14-6 | **resume 预算恢复**（同上追加）：resume 时 guard 从零起算（有意取舍），副作用 = 多次 resume 任务实际预算 N×budget、任务级审计碎片化。改为从项目 `logs/cost_report.json` 读历史 total\_tokens 注入新 guard；看板预算口径同步累计（修复「单任务总预算」语义稀释）                                                                            | P1  | 无        | 单测：含历史消耗的项目 resume 后 guard.summary 含历史用量；看板 total 为累计值  |
| M14-7 | **safe 模式 LLM 逻辑审查**（同上追加，规格 3.6.2 三件套补全）：AST 静态检查 + **LLM 逻辑审查** + 手动反馈——审查环节从未实现（全库零命中），safe 模式名实不符。实现：契约函数级审查（test\_model，聚焦 public\_api 而非全文件，控成本），verdict=fail → 进修复循环（附 issues）；LLM 调用/解析失败 → 降级放行（审查是增强非硬门禁，不阻塞流程）；新配置 `logic_review_enabled` 缺省开 | P1  | 无        | 单测：fail verdict 进修复循环；异常降级放行；auto 模式不审查（有真实执行反馈，避免冗余调用） |

### M15 · 接口门禁收敛率提升（本版核心）

> 根因（v0.5 实测取证）：契约声明函数式 API（read\_file 等 12 个导出），LLM 坚持生成
> 类式实现（FileManager/FileEncryptor 等），门禁报告仅 missing/extra 清单、无修改
> 指导，5 轮修复不收敛 → 全模块 FROZEN。这是收敛率 0% 的主因。

| #     | 任务                                                                                                                            | 优先级     | 依赖                                                                          | 验收标准                                            | <br /> | <br />                           |
| ----- | ----------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------- | ----------------------------------------------- | :----- | :------------------------------- |
| M15-1 | **拆分阶段契约风格约束**：split/initial\_proposal 提示词增加 API 风格约定——导出清单必须是「可直接调用的顶层符号」，附正反示例（✅ 函数式导出清单；❌ 把 API 收进类后类实例才可用）；spec 生成同步携带该约定 | P0      | 无                                                                           | 基准任务中契约导出与实现的风格错配率显著下降（人工抽检 3 个任务）              | <br /> | <br />                           |
| M15-2 | **门禁报告修改指导**：接口门禁失败输出从符号清单升级为「修改指导」——每个 missing 符号给出函数签名模板（从契约入参/出参推导）；extra 符号明确处置（改导出或删除），修复 LLM 一看即知怎么改                    | P0      | 无                                                                           | 单测：门禁报告含签名模板；v0.5 场景重放（file\_utils 类式实现）修复轮数 ≤2 | <br /> | <br />                           |
| M15-3 | **契约风格可配置**：`config.json` 增加 \`contract\_style: "function"                                                                    | "class" | "auto"\`（缺省 function）；auto = 首轮实现后按实际代码风格反推并回写契约（对齐 LLM 偏好，弃两端拉扯），风格决策落盘可审计 | P1                                              | M15-1  | 三种风格各有单测；auto 模式下风格冲突型 FROZEN 消除 |
| M15-4 | **修复循环上下文增强**：修复轮提示词携带接口地图全文 + 已通过模块对本模块 API 的调用示例（若有），让修复 LLM 看到「正确用法」而非仅符号名                                                 | P1      | M15-2                                                                       | 基准任务修复轮平均耗时/轮数下降；上下文 token 增幅 ≤15%              | <br /> | <br />                           |

### M16 · JS/TS 静态危险扫描（v0.4 已知边界遗留）

| #     | 任务                                                                                                                                                                                                 | 优先级 | 依赖 | 验收标准                                        |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | -- | ------------------------------------------- |
| M16-1 | **JS 静态扫描器**：`scan_dangerous_js`——require/import 黑名单（child\_process/fs 写操作/net/http/os）+ `eval`/`new Function`/动态 require 检测 + `node --check` 语法验证；接入 LocalExecutor 与 DockerExecutor 预扫描链（Node 任务） | P1  | 无  | 单测：恶意 JS 样本集全拦截；正常 Node 代码零误报；两执行器链路各一条集成测试 |

### M17 · Release 收口

| #     | 任务                                                                                                                                              | 优先级 | 依赖            | 验收标准                                               |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --- | ------------- | -------------------------------------------------- |
| M17-1 | **标准需求集与基准跑批**：定义 10 个标准需求（覆盖 CLI 工具/数据处理/文件操作/第三方库调用/陌生技术栈，难度 4-7），auto 模式全跑（Docker 可用则容器沙箱）；**模块通过率 ≥60% 为 Release 门槛**，不达标回炉 M15 后重跑（最多 2 轮） | P0  | M14 全部、M15 全部 | 10 任务跑批报告归档 `logs/bench_v1/`：逐任务通过率/修复轮/token/预算命中 |
| M17-2 | **Release 发布物**：版本号 1.0.0、CHANGELOG、README 口径更新（KPI 实测数）、exe 重建复验（≤80MB）、发布流水线全绿                                                                | P1  | M17-1         | Done 定义全过；`v1.0.0` 标签触发 Actions 成功                 |
| M17-3 | **文档口径同步**：文档目录.md / 规格对照说明 /（可选）投资人材料数据刷新（成功率从「待验证」更新为实测值）                                                                                     | P2  | M17-1         | 文档间数字一致，无「待验证」残留                                   |

***

## 四、里程碑

| 里程碑        | 内容                              | 准入                             |
| ---------- | ------------------------------- | ------------------------------ |
| V0 机制堵漏    | M14-1/2（\_shared 合并守卫 + 全局链接门禁） | v0.5 断裂场景复现被拦截；全量回归绿           |
| V1 收敛手段    | M14-3/4 + M15 全部                | file\_utils 场景重放 ≤2 轮收敛；风格单测全绿 |
| V2 中期抽查    | M16-1 + 基准 3 任务试跑               | 3 任务通过率 ≥50%（否则回炉 M15 再进 V3）   |
| V3 Release | M17 全量基准 + 发布                   | 10 任务通过率 ≥60%；Done 全过          |

***

## 五、依赖关系

```
M14-1 合并守卫 ── M14-2 链接门禁（依赖其落盘语义稳定）
M14-3 平台注入 ── M14-4 平台黑名单（同批落地，配置同源）
M15-1 风格约束 ── M15-2 报告指导 ── M15-4 上下文增强（层层递进）
M15-3 风格可配置（独立，消费 M15-1 的风格定义）
M16-1 JS 扫描（独立）
M17-1 基准跑批 ── 依赖 M14/M15/M16 全部（验收它们的效果）
M17-2/3 发布 ── 依赖 M17-1 达标
```

***

## 六、风险与注意事项

| 风险                                        | 应对                                                              |
| ----------------------------------------- | --------------------------------------------------------------- |
| M15 提示词改进对收敛率的实际效果不确定                     | V2 中期抽查设 50% 准入线，不达标回炉，避免带病进 V3                                 |
| M14-1 合并守卫的 AST 合并产生「僵尸函数」（新版本刻意删除的符号被保留） | 同名内容变更采用新版；删除场景 LLM 须显式标记（提示词约定 `# DELETED` 注释约定），无标记默认保留       |
| M14-2 全局链接门禁拖慢开发循环（每模块全项目解析）              | AST 解析毫秒级 + 增量缓存（仅解析变更文件）；实测超 500ms 再优化                         |
| 基准跑批 API 成本（10 任务 × auto ≈ 50 万 token 量级） | 单任务预算闸门不变（500k）；跑批分批执行，单批成本落盘可中止                                |
| LLM 输出质量受模型档位影响（v0.5 曾因余额切到轻量档）           | 基准跑批前 ModelClient 探活三模型；跑批配置落盘（模型/版本）保证可复现                      |
| M15-3 auto 风格回写与「决策归 LLM」哲学冲突             | 风格是工程约束非业务决策：auto 仅在首轮实现后一次性回写并留审计记录，用户可锁定 function/class 关闭自适应 |

***

## 七、Done 的定义

- [ ] M14-1/2：v0.5 断裂场景（\_shared 重写丢函数）复现并被确定性拦截

- [ ] M14-3/4：fcntl 场景重跑产出平台可用实现；平台黑名单扫描生效

- [ ] M15：file\_utils 风格冲突场景重放 ≤2 轮收敛（v0.5 为 5 轮不收敛）

- [ ] M16：JS 恶意样本全拦截，两执行器链路集成测试绿

- [ ] **M17-1：标准需求集 10 任务 × auto，模块通过率 ≥60%，报告归档**

- [ ] 预算命中率：基准任务超支率 <10%

- [ ] 全量测试基线 833 项无回退，新增 ≥47 项（≥880）

- [ ] 版本号 1.0.0；CHANGELOG/README/文档目录口径一致；`v1.0.0` 标签 Actions 全绿

***

## 八、v1.1+ 备忘（2026-09-03 组件评审，明确不做进 v1.0）

以下小项经评审判断收益/成本比不足或纯打磨，记录在此供 v1.1+ 择取：

- **SafeExecutor 运行指令占位符**：`cd projects/<项目目录>/code` 为字面模板，
  executor 层拿不到项目实际路径，用户需自行替换。修法 = executor 接口增加
  上下文参数（涉及接口变更），收益仅 UX 打磨。

- **SafeExecutor docstring 名实对齐**：类注释声称「AST 静态检查」在本类，
  实际在上游门禁链（v1.0 V0 后门禁链已含链接门禁）。纯注释修正，
  随下次触碰该文件时顺手改。

- **BudgetGuard 单次调用级超支**：check-before/record-after 模式下，单次
  大调用可越过预算上界一个受控额度（输出已受 8k 上限约束）才中止。完全
  堵死需调用前预估输入 token（估算不准反而引入误拦），收益小不做。

<br />
